% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gmm_model.R
\name{likelihoodgmm}
\alias{likelihoodgmm}
\title{Compute the mixture density for samples}
\usage{
likelihoodgmm(model = NULL, X = NULL)
}
\arguments{
\item{model}{Gaussian mixture model returned by \code{\link[=gmm]{gmm()}}.}

\item{X}{Numeric vector or matrix of samples.}
}
\value{
Numeric vector of length \code{nrow(X)} containing \code{p(x_i)} for each sample row.
Values are nonnegative density values and do not sum to one over the sample.
They are pointwise density evaluations, so their scale depends on the
dimension and covariance matrices of the mixture.
}
\description{
Evaluates the Gaussian mixture density at each supplied sample.
}
\details{
For observation \code{x_i}, the returned value is
\deqn{p(x_i) = \sum_{k=1}^K w_k \phi(x_i; \mu_k, \Sigma_k).}
The component densities are combined on the log scale and then converted
back to ordinary density values.

This function returns the density itself, not a log density. Use it for plots
or simple likelihood checks. For MCMC transitions such as \code{\link[=mala]{mala()}} and
\code{\link[=grwmetrop]{grwmetrop()}}, pass a separate \code{log_p} function because those transitions
work on the log-density scale.

The function sits beside \code{\link[=posteriorgmm]{posteriorgmm()}} and \code{\link[=scorefunctiongmm]{scorefunctiongmm()}}. All three
evaluate the same mixture at supplied sample rows, but they answer different
questions: \code{likelihoodgmm()} returns the marginal density \code{p(x)},
\code{\link[=posteriorgmm]{posteriorgmm()}} returns component responsibilities conditional on \code{x}, and
\code{\link[=scorefunctiongmm]{scorefunctiongmm()}} returns the gradient of \verb{log p(x)} needed by Stein
algorithms.
}
\examples{
# compute likelihood for a default 1-d gaussian mixture model
# and dataset generated from it
model <- gmm()
X <- rgmm(model)
p <- likelihoodgmm(model = model, X = X)
}
