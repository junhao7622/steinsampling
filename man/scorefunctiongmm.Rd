% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gmm_model.R
\name{scorefunctiongmm}
\alias{scorefunctiongmm}
\title{Compute the score of a Gaussian mixture density}
\usage{
scorefunctiongmm(model = NULL, X = NULL)
}
\arguments{
\item{model}{Gaussian mixture model returned by \code{\link[=gmm]{gmm()}}.}

\item{X}{Numeric vector or matrix of samples.}
}
\value{
Numeric matrix with \code{nrow(X)} rows and \code{model$d} columns. Row \code{i} is
\verb{nabla_x log p(x_i)}, the score vector used by Stein discrepancies and
score-based samplers. The matrix has the same row order as the supplied
sample matrix, which is important because KSD, FSSD, Stein thinning, and
Stein Points pair each sample row with its corresponding score row.
}
\description{
The score is the gradient of the log mixture density with respect to \code{x}.
}
\details{
For a mixture, the score is a weighted average of the component scores:
\deqn{
\nabla_x \log p(x_i) =
\sum_{k=1}^K P(Z_i = k | x_i)
\left[-\Sigma_k^{-1}(x_i - \mu_k)\right].
}
The weights in this average are the posterior component probabilities
returned by \code{\link[=posteriorgmm]{posteriorgmm()}}.

This is the Gaussian mixture helper most directly connected to the Stein
routines. \code{\link[=ksd_u_test]{ksd_u_test()}}, \code{\link[=fssd_test]{fssd_test()}}, \code{\link[=stein_points]{stein_points()}}, and related
functions need \verb{function(X)} returning the score matrix; this function
computes that matrix for one supplied batch.

If the target were a single Gaussian, the score would be one linear term
\code{-Sigma^{-1}(x - mu)}. For a mixture, the function computes that linear
score for every component and averages the component scores using the
posterior probabilities from \code{\link[=posteriorgmm]{posteriorgmm()}}. This is why both the density
calculation and the inverse covariance matrices appear in the implementation.
}
\examples{
# Compute score for a given gaussianmixture model and dataset
model <- gmm()
X <- rgmm(model)
score <- scorefunctiongmm(model = model, X = X)
}
