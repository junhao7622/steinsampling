% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stein_point_mcmc.R
\name{sp_mcmc}
\alias{sp_mcmc}
\title{Stein Point MCMC using the greedy/MCMC selection loop from Section 3.1.}
\usage{
sp_mcmc(
  score_function,
  log_p,
  kernel,
  n_points,
  d,
  mcmc = c("grw", "mala", "rwm"),
  criterion = c("last", "rand", "infl"),
  m_seq,
  h = NULL,
  Sigma = NULL,
  x_init,
  seed = NULL,
  transition_fn = NULL,
  proposal_fn = NULL,
  n_eval_fn = NULL,
  criterion_args = list()
)
}
\arguments{
\item{score_function}{function(X) returning grad log p, X is n x d.}

\item{log_p}{function(X) returning log p, length n.}

\item{kernel}{a \code{\link[=stein_kernel]{stein_kernel()}} object, a function
\code{kernel(X, S_X, Y, S_Y)}, or a list with \code{k0_matrix}.}

\item{n_points}{integer, total points to generate.}

\item{d}{integer, state dimension.}

\item{mcmc}{"grw" (Gaussian random-walk Metropolis), "mala",
or alias "rwm".}

\item{criterion}{start-point criterion: "last", "rand", "infl", or a
function taking an \code{sp_mcmc_state} object and returning one selected index.}

\item{m_seq}{chain length per greedy index; scalar, length n_points,
or length n_points - 1 for the updates j = 2, ..., n_points.}

\item{h}{MALA/GRW step size. For both built-in kernels, the
proposal covariance is \code{h * Sigma}; if \code{h} is NULL for GRW it defaults to 1.}

\item{Sigma}{covariance/preconditioner matrix Sigma in Appendix A.5.
If NULL, the identity matrix is used by the built-in kernels.}

\item{x_init}{Fixed first point x_1.}

\item{seed}{optional RNG seed.}

\item{transition_fn}{optional Markov transition function with signature
\verb{(log_p, score_function, x0, h, Sigma, m_iter)}.  Defaults to the built-in
kernel selected by \code{mcmc}.}

\item{proposal_fn}{optional function returning per-step proposal settings
with signature \verb{(j, X_curr, h, Sigma, mcmc)}.  Return a list containing
replacement \code{h} and/or \code{Sigma}.}

\item{n_eval_fn}{optional function returning the per-step evaluation
count with signature \verb{(chain, cand_X, cand_D, m_j, mcmc, j, X_curr)}.}

\item{criterion_args}{optional list of extra arguments passed to a custom
\code{criterion} function.}
}
\value{
list with \code{X}, \code{D}, \code{ksd}, per-step evaluation counts, chain jump
diagnostics, and S3 class c("sp_mcmc", "stein_points").
}
\description{
\code{x_init} is the fixed initial point \code{x_1}.  For \verb{j = 2, ..., n}, a
\code{P}-invariant Markov chain is initialized at the selected current point and
the next point is chosen from the realized path.
}
\examples{
score <- function(X) -as.matrix(X)
log_p <- function(X) -0.5 * rowSums(as.matrix(X)^2)
kernel <- stein_kernel(type = "gaussian_rbf", h = 1)
sp_mcmc(score, log_p, kernel, n_points = 2, d = 1, m_seq = 2, x_init = 0)
}
