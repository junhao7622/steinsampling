% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stein_point_mcmc.R
\name{summary.sp_mcmc}
\alias{summary.sp_mcmc}
\alias{print.summary.sp_mcmc}
\title{Summarize an SP-MCMC point set}
\usage{
\method{summary}{sp_mcmc}(object, ...)

\method{print}{summary.sp_mcmc}(x, ...)
}
\arguments{
\item{object}{A \code{"sp_mcmc"} object.}

\item{...}{Ignored.}

\item{x}{A \code{"summary.sp_mcmc"} object.}
}
\value{
A \code{"summary.sp_mcmc"} object extending \code{"summary.stein_points"} with
\code{transition}, \code{criterion}, \code{m_seq}, \code{h}, \code{accept_rate}, \code{n_repeated}, and
\code{counts}. The print method returns \code{x} invisibly.
}
\description{
Adds SP-MCMC transition, selection, acceptance, and evaluation
diagnostics to \code{\link[=summary.stein_points]{summary.stein_points()}}.
}
\details{
\code{accept_rate} is weighted by the number of transitions in each path;
paths without acceptance diagnostics are omitted. \code{n_repeated} counts
selected points that repeat an earlier one; a candidate path contains its own
starting state, so repeats are expected. \code{h} is the initial
step size, before any per-step proposal changes. \code{counts} contains the
\code{log_p}, \code{score}, and \code{candidate_score} column totals; \code{n_eval_total}
reports their total once.
}
\examples{
score_function <- function(x) -as.matrix(x)
log_p <- function(x) -0.5 * rowSums(as.matrix(x)^2)
fit <- sp_mcmc(score_function, log_p,
               stein_kernel(type = "gaussian_rbf", h = 1),
               n_points = 4, d = 1, m_seq = 2, h = 0.5, x_init = 0)
summary(fit)
}
\seealso{
\code{\link[=summary.stein_points]{summary.stein_points()}}, \code{\link[=sp_mcmc]{sp_mcmc()}}
}
