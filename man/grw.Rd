% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stein_point_mcmc.R
\name{grw}
\alias{grw}
\title{Run the Gaussian random-walk transition used by SP-MCMC}
\usage{
grw(log_p, score_function, x0, h = NULL, Sigma = NULL, m_iter)
}
\arguments{
\item{log_p}{Function returning log density values.}

\item{score_function}{Unused score function argument kept for transition
interface compatibility.}

\item{x0}{Initial state vector.}

\item{h}{Optional positive step-size multiplier.}

\item{Sigma}{Symmetric positive-definite proposal covariance or
preconditioning matrix.}

\item{m_iter}{Number of returned chain rows, including the initial state in
row 1. The function therefore makes \code{m_iter - 1} Markov transitions.}
}
\value{
Output from \code{\link[=grwmetrop]{grwmetrop()}}, with chain states, log densities, acceptance
indicators, and evaluation counts. The score matrix is absent by design;
SP-MCMC computes candidate scores afterward when needed.
}
\description{
This is a convenience wrapper around \code{\link[=grwmetrop]{grwmetrop()}} using proposal covariance
\code{h * Sigma}, with \code{h = 1} when omitted.
}
\details{
The SP-MCMC main loop passes the log density, score function, current state,
step size, proposal matrix, and chain length to every transition. The
argument \code{score_function} is therefore present only for interface
compatibility with \code{\link[=mala]{mala()}} and custom transition functions. The random-walk
proposal itself is \eqn{N(x, h Sigma)} and does not use the score.

Use this wrapper, rather than \code{\link[=grwmetrop]{grwmetrop()}}, when supplying a custom
transition-like function to \code{\link[=sp_mcmc]{sp_mcmc()}} or when comparing GRW and MALA under
the same call signature.
}
\examples{
score <- function(X) -as.matrix(X)
log_p <- function(X) -0.5 * rowSums(as.matrix(X)^2)
grw(log_p, score, x0 = 0, h = 0.1, m_iter = 3)
}
