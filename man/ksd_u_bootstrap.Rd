% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ksd_u_test.R
\name{ksd_u_bootstrap}
\alias{ksd_u_bootstrap}
\title{Centered multinomial bootstrap for KSD-U}
\usage{
ksd_u_bootstrap(
  K0,
  nboot = 1000,
  W_mat = NULL,
  boot_method = "multinomial_centered"
)
}
\arguments{
\item{K0}{Numeric \eqn{n\times n} Stein-kernel matrix, such as the output of
\code{\link[=ksd_uq_matrix]{ksd_uq_matrix()}}.}

\item{nboot}{Positive number of bootstrap draws. Ignored when \code{W_mat} is
supplied.}

\item{W_mat}{Optional numeric \eqn{n\times B} matrix of centered weights.
Row \eqn{i} corresponds to observation \eqn{i}; column \eqn{b} specifies
bootstrap draw \eqn{b}. If \code{NULL}, the weights above are generated.}

\item{boot_method}{Bootstrap method. Currently only
\code{"multinomial_centered"} is supported.}
}
\value{
Numeric vector containing \eqn{T_n^{*(1)},\ldots,T_n^{*(B)}}.
}
\description{
Centered multinomial bootstrap for KSD-U
}
\details{
For bootstrap draw \eqn{b}, let
\deqn{w_i^{(b)}=\frac{N_i^{(b)}}{n}-\frac{1}{n},\qquad
(N_1^{(b)},\ldots,N_n^{(b)})\sim
\operatorname{Multinomial}\left(n;\frac1n,\ldots,\frac1n\right).}
The returned draw is
\deqn{T_n^{*(b)}=n\sum_{i\ne j}w_i^{(b)}w_j^{(b)}K_{ij}.}
The diagonal is excluded. The draws are on the same scale as
\code{\link[=ksd_u_statistic]{ksd_u_statistic()}}. This function does not compute a p-value.
}
\examples{
U <- matrix(c(1, 0.2, 0.3, 0.2, 1, 0.4, 0.3, 0.4, 1), 3, 3)
ksd_u_bootstrap(U, nboot = 5)
}
