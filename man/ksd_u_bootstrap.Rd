% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ksd_u_test.R
\name{ksd_u_bootstrap}
\alias{ksd_u_bootstrap}
\title{Bootstrap the KSD U-statistic from a Stein-kernel matrix}
\usage{
ksd_u_bootstrap(
  U_mat,
  nboot = 1000,
  W_mat = NULL,
  boot_method = "multinomial_centered"
)
}
\arguments{
\item{U_mat}{Numeric square matrix of pairwise Stein kernel values.}

\item{nboot}{Number of bootstrap samples.}

\item{W_mat}{Optional \verb{n x B} centered multinomial bootstrap weight matrix,
with one row per observation and one column per bootstrap draw.}

\item{boot_method}{Bootstrap method. Currently only
\code{"multinomial_centered"} is supported.}
}
\value{
Numeric vector of length \code{nboot} when \code{W_mat} is not supplied, or length
\code{ncol(W_mat)} when weights are supplied. Each entry is one bootstrap
U-statistic on the same scale as \code{\link[=ksd_u_statistic]{ksd_u_statistic()}}, so it can be compared
directly with the observed statistic.
}
\description{
Generates or accepts centered multinomial weights and applies them to the
U-statistic matrix. This is the bootstrap part of the Liu et al. KSD test
separated from matrix construction.
}
\details{
For each bootstrap weight vector \code{w}, the bootstrap statistic is computed as
\deqn{\sum_{i,j} w_i U_{ij} w_j,}
with the diagonal of \code{U_mat} set to zero. These values are on the same scale
as \code{\link[=ksd_u_statistic]{ksd_u_statistic()}}. The default weights are centered multinomial
proportions, \code{N_i / n - 1 / n}, matching Eq. 16 of Liu, Lee, and Jordan
(2016). The paper compares \code{n} times the observed and bootstrap statistics;
this package leaves both on the unmultiplied U-statistic scale, which gives
the same p-value. Supplying \code{W_mat} lets users reuse exactly the same
bootstrap weights across kernels or experiments.

This function sits below \code{\link[=ksd_u_test]{ksd_u_test()}} and above the raw weight generator.
It exists so users can separate randomness in the bootstrap weights from the
deterministic matrix calculation. Supplying \code{W_mat} is the design point:
the same matrix of weights can be reused across several \code{U_mat} objects,
which is useful for paired comparisons of kernels or bandwidths.

The parallel bootstrap helper is \code{\link[=ksd_v_bootstrap]{ksd_v_bootstrap()}}. The input matrix plays
the same role, but the weights have different meanings: \code{ksd_u_bootstrap()}
uses centered multinomial proportions for the degenerate U-statistic in Liu
et al., whereas \code{\link[=ksd_v_bootstrap]{ksd_v_bootstrap()}} uses wild-bootstrap sign sequences for
the V-statistic. To move from this function to the V version, keep a
Stein-kernel matrix but supply sign weights or choose a sign-generation
method instead of centered multinomial weights.
}
\examples{
U <- matrix(c(1, 0.2, 0.3, 0.2, 1, 0.4, 0.3, 0.4, 1), 3, 3)
ksd_u_bootstrap(U, nboot = 5)
}
