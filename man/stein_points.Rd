% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stein_points.R
\name{stein_points}
\alias{stein_points}
\title{Stein Points sequence.}
\usage{
stein_points(
  score_function,
  kernel,
  n_points,
  d,
  optimizer,
  method = c("greedy", "herding"),
  log_p = NULL,
  x_init = NULL,
  c2 = NULL,
  truncation = c("none", "upper", "lower", "linear"),
  seed = NULL
)
}
\arguments{
\item{score_function}{Function returning scores, \verb{n x d}, for candidate rows.}

\item{kernel}{Built-in Stein kernel, S3 object with \code{stein_kernel_matrix()},
function \code{kernel(X, S_X, Y, S_Y)}, or list with \code{k0_matrix}. Custom
kernels should return \code{k0(X_i, Y_j)} and should be symmetric Stein kernels.}

\item{n_points}{Number of points to select.}

\item{d}{Dimension of each point.}

\item{optimizer}{Function taking \verb{(objective, X_curr)} and returning
\code{x_min}, \code{d_min}, \code{f_min}, and \code{n_eval}.}

\item{method}{\code{"greedy"} minimises \verb{k0(x,x) + 2 sum_i k0(x_i,x)};
\code{"herding"} minimises \verb{sum_i k0(x_i,x)}.}

\item{log_p}{Optional log density used to choose the first point.}

\item{x_init}{Optional first point. If supplied, \code{log_p} is not used.}

\item{c2}{Positive truncation constant when \code{truncation != "none"}.}

\item{truncation}{Optional Theorem 1 candidate filter based on
\code{k0(x,x) <= R_j^2}. The main Stein Herding algorithm uses \code{"none"}.}

\item{seed}{Optional local RNG seed.}
}
\value{
List with selected points \code{X}, scores \code{D}, running \code{ksd}, evaluation
counts, and method settings.
}
\description{
Selects points one at a time. The first point is \code{x_init}, or the maximiser
of \code{log_p}. Later points minimise either the greedy or herding Stein
objective against the points already selected.
}
\examples{
score <- function(X) -as.matrix(X)
log_p <- function(X) -0.5 * rowSums(as.matrix(X)^2)
kernel <- stein_kernel(type = "gaussian_rbf", h = 1)
opt <- fmin_grid(lb = -1, ub = 1, n0 = 3, grow = FALSE)
stein_points(score, kernel, n_points = 2, d = 1, optimizer = opt,
             log_p = log_p)
}
