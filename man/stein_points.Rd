% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stein_points.R
\name{stein_points}
\alias{stein_points}
\title{Build a Stein Points sequence by greedy KSD minimization}
\usage{
stein_points(
  score_function,
  kernel,
  n_points,
  d,
  optimizer,
  method = c("greedy", "herding"),
  log_p = NULL,
  x_init = NULL,
  c2 = NULL,
  truncation = c("none", "upper", "lower", "linear"),
  seed = NULL
)
}
\arguments{
\item{score_function}{Function returning target scores for candidate rows.}

\item{kernel}{Built-in Stein kernel, compatible custom kernel, function
\code{kernel(X, S_X, Y, S_Y)}, or list with \code{k0_matrix}.}

\item{n_points}{Number of points to select.}

\item{d}{Dimension of each point.}

\item{optimizer}{Function taking \verb{(objective, X_curr, t)} and returning
\code{x_min}, \code{d_min}, \code{f_min}, and \code{n_eval}.}

\item{method}{Point-selection rule: \code{"greedy"} or \code{"herding"}.}

\item{log_p}{Optional log density used to choose the first point.}

\item{x_init}{Optional first point. If supplied, \code{log_p} is not used.}

\item{c2}{Positive truncation constant when \code{truncation != "none"}.}

\item{truncation}{Optional filter that removes candidates whose self-kernel
value is too large. Use \code{"none"} for the basic Stein Points algorithm.}

\item{seed}{Optional local RNG seed.}
}
\value{
A list with:
\itemize{
\item \code{X}: \verb{n_points x d} matrix of selected point locations.
\item \code{D}: \verb{n_points x d} matrix of target scores evaluated at \code{X}.
\item \code{ksd}: numeric vector; \code{ksd[j]} is the square root of the empirical KSD
double average for the first \code{j} selected points, using the same \code{k0}
that drove selection.
\item \code{n_eval}: number of objective or score evaluations charged at each
selection step. This is kept separate from \code{ksd} because different
optimizers can reach similar KSD values with very different budgets.
\item \code{cum_n_eval}: cumulative sum of \code{n_eval}, used for budget-vs-accuracy
comparisons across \code{\link[=fmin_grid]{fmin_grid()}}, \code{\link[=fmin_mc]{fmin_mc()}}, \code{\link[=fmin_nm]{fmin_nm()}}, and SP-MCMC.
\item \code{method}: \code{"greedy"} or \code{"herding"}.
\item \code{kernel}: kernel object or compatible kernel supplied by the user.
\item \code{truncation}, \code{c2}: truncation rule and constant used for candidate
filtering.
}

The list is returned instead of only the point matrix because later analysis
often needs the scores \code{D}, the KSD path, and evaluation counts. For example,
\code{\link[=stein_codescent]{stein_codescent()}} can refine \code{X}, \code{D} avoids recomputing target scores,
\code{ksd} shows the discrepancy path, and \code{n_eval}/\code{cum_n_eval} allow
comparisons between grid, Monte Carlo, Nelder-Mead, and SP-MCMC under the
same evaluation budget.
}
\description{
Implements the Stein Points construction of Chen et al. The function builds a
point set whose empirical distribution is meant to approximate the target by
making the kernel Stein discrepancy small. The point-selection rule itself
introduces no randomness, but reproducibility depends on the numerical
search: grid search is deterministic, whereas Monte Carlo and randomly
initialized Nelder-Mead searches are stochastic unless their seed is fixed.
}
\details{
In the paper, the KSD of a point set is written through the Stein kernel
values \code{k0(x_i, x_l)}. With the package notation, for selected points
\eqn{x_1,\ldots,x_m},
\deqn{KSD_m^2 = \frac{1}{m^2}
       \sum_{a=1}^m \sum_{b=1}^m k0(x_a, x_b).}
The kernel \code{k0} is assembled by \code{\link[=stein_kernel_matrix]{stein_kernel_matrix()}} from the target
score \eqn{s_p(x)=\nabla_x\log p(x)} and the selected base kernel. The first
point is either supplied by \code{x_init} or chosen by maximizing \code{log_p}, which
follows the paper's default choice of starting near a mode of the target.
After points \eqn{x_1,\ldots,x_{j-1}} have been selected, the greedy rule chooses
the next point by minimizing
\deqn{\frac{1}{2} k0(x, x) + \sum_{i=1}^{j-1} k0(x_i, x).}
The implementation scores candidates with the doubled but equivalent form
\deqn{k0(x, x) + 2 \sum_{i=1}^{j-1} k0(x_i, x),}
which has the same minimizer and is exactly the amount added to the running
double sum \eqn{\sum_{a,b\le j} k0(x_a,x_b)}. The herding rule drops the
self term in the selection objective and minimizes
\deqn{\sum_{i=1}^{j-1} k0(x_i, x).}
After a herding point is chosen, the package still adds both cross
interactions and the new self-interaction to the KSD diagnostic, so \code{ksd}
remains comparable between greedy and herding runs.

The hard part of the paper algorithm is the global search over \code{x}. This
function decouples that search from the Stein objective. The \code{optimizer}
receives an objective function, the current point matrix \code{X_curr}, and the
iteration index \code{t}; it must
return a list containing \code{x_min} (the selected row), \code{d_min} (the score at
that row), \code{f_min} (the objective value), and \code{n_eval} (the number of target
or candidate evaluations it used). The helpers \code{\link[=fmin_grid]{fmin_grid()}}, \code{\link[=fmin_mc]{fmin_mc()}},
and \code{\link[=fmin_nm]{fmin_nm()}} implement the grid, Monte Carlo, and Nelder-Mead searches
used in the Stein Points experiments.

The output records both the selected points and the running KSD diagnostic.
\code{X} is an \verb{n_points x d} matrix, \code{D} is the matching score matrix with row
\code{D[j, ] = s_p(X[j, ])}, and
\deqn{ksd[j] =
\left\{\frac{1}{j^2}
\sum_{a=1}^j \sum_{b=1}^j k0(x_a,x_b)\right\}^{1/2}.}
If \code{truncation} is not \code{"none"}, candidates whose self-kernel value
\code{k0(x,x)} is outside the paper's admissible region are rejected before the
best one is chosen, matching the truncated variants used in the convergence
results.

The design separates the Stein objective from the numerical search. This is
why \code{optimizer} is an argument and why \code{\link[=fmin_grid]{fmin_grid()}}, \code{\link[=fmin_mc]{fmin_mc()}}, and
\code{\link[=fmin_nm]{fmin_nm()}} return optimizer functions rather than points directly. The
Stein objective knows the current selected points and the score function; the
optimizer only decides how to search for a good next candidate.

The kernel input sits at the same layer as \code{\link[=stein_kernel_matrix]{stein_kernel_matrix()}}. The three
Stein Points kernel families implemented here are:
\deqn{k_{\mathrm{IMQ}}(x,y) = (c^2 + r(x,y))^\beta,\quad
      r(x,y)=(x-y)^T M (x-y),}
from \code{\link[=stein_kernel]{stein_kernel()}}, usually with \eqn{\beta\in(-1,0)};
\deqn{k_{\log}(x,y) =
      (\alpha+\log(1+||x-y||^2))^\beta,}
from \code{\link[=stein_kernel_inverse_log]{stein_kernel_inverse_log()}}, with the paper's inverse-log choice
recovered at \eqn{\beta=-1}; and
\deqn{k_{\mathrm{score}}(x,y) =
      (\alpha+||s_p(x)-s_p(y)||^2)^\beta,}
from \code{\link[=stein_kernel_imq_score]{stein_kernel_imq_score()}}. Moving sideways among these choices keeps
the same greedy or herding point-selection loop, but changes what distance
the base kernel uses before the common Stein-kernel assembly: position
distance for IMQ, logarithmic position distance for inverse-log, and
score-vector distance for score-distance IMQ. The score-distance kernel also
requires a Hessian evaluator because derivatives of
\eqn{s_p(x)=\nabla_x\log p(x)} enter \code{k0}.
}
\examples{
score <- function(X) -as.matrix(X)
log_p <- function(X) -0.5 * rowSums(as.matrix(X)^2)
kernel <- stein_kernel(type = "gaussian_rbf", h = 1)
opt <- fmin_grid(lb = -1, ub = 1, n0 = 3, grow = FALSE)
stein_points(score, kernel, n_points = 2, d = 1, optimizer = opt,
             log_p = log_p)
}
