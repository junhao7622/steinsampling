% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stein_points_alternative_kernel.R
\name{stein_kernel_imq_score}
\alias{stein_kernel_imq_score}
\title{Create a score-distance IMQ Stein kernel}
\usage{
stein_kernel_imq_score(alpha = 1, beta = -0.5, hess_log_p)
}
\arguments{
\item{alpha}{Positive offset parameter.}

\item{beta}{Exponent in \verb{(-1, 0)}.}

\item{hess_log_p}{Function returning an \verb{n x d x d} Hessian array.}
}
\value{
A \code{SteinKernel} object for the score-distance IMQ kernel.
}
\description{
Constructs an IMQ kernel from distances between target-score vectors.
}
\details{
With \eqn{s_p(x)=\nabla_x\log p(x)}, the base kernel is
\deqn{k(x,y)=(\alpha+||s_p(x)-s_p(y)||^2)^\beta.}
Its Stein derivatives require \code{hess_log_p}, which returns an \verb{n x d x d}
array containing one Hessian of the target log density per row of \code{X}.
Preconditioning is not supported.
}
\examples{
hess_log_p <- function(X) array(-1, dim = c(nrow(as.matrix(X)), 1, 1))
stein_kernel_imq_score(alpha = 1.5, beta = -0.25,
                      hess_log_p = hess_log_p)
}
