% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gmm_model.R
\name{densitygmm}
\alias{densitygmm}
\title{Evaluate a Gaussian mixture density}
\usage{
densitygmm(model = NULL, X = NULL)
}
\arguments{
\item{model}{Gaussian mixture model returned by \code{\link[=gmm]{gmm()}}.}

\item{X}{Finite numeric vector or matrix. For a one-dimensional model, a
vector represents multiple observations. For a multivariate model, a
length-\code{d} vector represents one observation. Matrix rows are observations.}
}
\value{
A numeric vector containing one ordinary density value per interpreted
observation in \code{X}.
}
\description{
Returns the density of a \code{\link[=gmm]{gmm()}} object at each supplied observation.
}
\details{
For each observation \eqn{x_i},
\deqn{p(x_i) = \sum_{k=1}^K w_k \phi(x_i; \mu_k, \Sigma_k).}
The function returns ordinary density values, not log densities.
}
\examples{
model <- gmm()
X <- rgmm(model)
p <- densitygmm(model = model, X = X)
}
