% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stein_point_mcmc.R
\name{sp_mcmc_state}
\alias{sp_mcmc_state}
\title{Store the current SP-MCMC state for a start rule}
\usage{
sp_mcmc_state(
  j,
  X,
  D,
  K0,
  ksd = NULL,
  n_eval = NULL,
  counts = NULL,
  selected_index = NULL,
  kernel = NULL,
  mcmc = NULL,
  criterion = NULL
)
}
\arguments{
\item{j}{Current greedy index.}

\item{X}{Current selected point matrix.}

\item{D}{Current score matrix.}

\item{K0}{Current Stein kernel matrix.}

\item{ksd}{Optional running KSD values.}

\item{n_eval}{Optional evaluation counts.}

\item{counts}{Optional detailed evaluation-count matrix.}

\item{selected_index}{Optional selected start indices.}

\item{kernel}{Kernel used by SP-MCMC.}

\item{mcmc}{MCMC transition name.}

\item{criterion}{Criterion label.}
}
\value{
A list of class \code{"sp_mcmc_state"} with the fields supplied as arguments:
current points \code{X}, scores \code{D}, Stein-kernel matrix \code{K0}, current step \code{j},
running diagnostics, and method labels. It is not a result object; it is a
snapshot passed to a start-point criterion so the criterion can decide where
the next short MCMC candidate chain should start without recomputing scores
or pairwise kernel values.
}
\description{
Creates the state object passed to custom SP-MCMC start-point criteria. The
criterion uses this object to decide which selected point should initialize
the next short MCMC chain.
}
\details{
The state contains the current point set, scores, and Stein kernel matrix, as
well as diagnostic vectors accumulated so far. The matrix field satisfies
\code{K0[a, b] = k0(X[a, ], X[b, ])}, using the same target scores stored in \code{D}.
In the paper, the start rule is one of LAST, RAND, or INFL. This object makes
those rules explicit: LAST uses \code{state$X}, RAND samples from its rows, and
INFL uses \code{state$K0}. A custom rule should inspect the fields it needs and
return one row index from \code{state$X}.

This constructor is exported for users who write custom SP-MCMC start rules.
The main \code{\link[=sp_mcmc]{sp_mcmc()}} loop creates this object internally before every short
chain. By exposing the same object, the package lets custom rules be tested
outside the full algorithm.
}
\examples{
sp_mcmc_state(j = 2, X = matrix(0, ncol = 1), D = matrix(0, ncol = 1),
              K0 = matrix(1))
}
