% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fssd_test.R
\name{fssd_test}
\alias{fssd_test}
\title{Finite Set Stein Discrepancy (FSSD) Goodness-of-Fit Test}
\usage{
fssd_test(
  X,
  score_function,
  variant = c("opt", "rand"),
  J = 5,
  nboot = 2000,
  kernel = c("gaussian_rbf", "imq"),
  scaling = NULL,
  train_ratio = 0.2,
  eval_on = c("test", "all"),
  gamma = 1e-04,
  maxit = 100,
  locs_bounds_frac = 10,
  scale_lower = 0.1,
  scale_upper = 10000,
  seed = NULL,
  median_max_samples = 2000,
  median_use_sampling = TRUE,
  median_seed = 123,
  imq_beta = -0.5
)
}
\arguments{
\item{X}{Numeric vector or matrix of samples (\verb{n x d}).}

\item{score_function}{Function returning \verb{nabla_x log p(x)} with shape
\verb{n x d} (or length-\code{n} when \code{d = 1}).}

\item{variant}{\code{"opt"} learns test locations and kernel scale on a training
split; \code{"rand"} draws test locations from a Gaussian fitted to the data.}

\item{J}{Number of test locations.}

\item{nboot}{Null-simulation draws for the chi-square mixture.}

\item{kernel}{\code{"gaussian_rbf"}, \code{"imq"}, or a \code{SteinKernel} object.}

\item{scaling}{Optional positive squared kernel scale. \code{NULL} uses the
median squared-distance heuristic.}

\item{train_ratio}{FSSD-opt training fraction.}

\item{eval_on}{FSSD-opt evaluation sample. \code{"test"} is the methodologically
valid held-out test described in the paper. \code{"all"} evaluates on the full
sample after tuning and is diagnostic only.}

\item{gamma}{Fixed FSSD-opt regularizer added outside the square root:
\code{FSSD^2 / (sqrt(var_H1) + gamma)}.}

\item{maxit}{FSSD-opt L-BFGS-B iterations.}

\item{locs_bounds_frac}{Test-location box expansion in standard deviations.}

\item{scale_lower, scale_upper}{Absolute bounds for the optimized squared
kernel scale.}

\item{seed}{Optional RNG seed for data split and random locations.}

\item{median_max_samples, median_use_sampling, median_seed}{Forwarded to
\code{find_median_distance}.}

\item{imq_beta}{IMQ exponent.}
}
\value{
An object of class \code{htest}.
}
\description{
Dispatcher between FSSD-rand and FSSD-opt.
}
\examples{
X <- matrix(rnorm(8), ncol = 1)
score_function <- function(x) -as.matrix(x)
fssd_test(X, score_function, variant = "rand", J = 1, nboot = 10)
}
