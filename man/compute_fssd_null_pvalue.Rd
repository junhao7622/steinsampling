% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fssd_test.R
\name{compute_fssd_null_pvalue}
\alias{compute_fssd_null_pvalue}
\title{Simulate the FSSD null distribution}
\usage{
compute_fssd_null_pvalue(tau_matrix, fssd_stat, n_simulations = 2000)
}
\arguments{
\item{tau_matrix}{Numeric FSSD feature matrix.}

\item{fssd_stat}{Observed FSSD statistic.}

\item{n_simulations}{Number of null draws to simulate.}
}
\value{
A list with three entries:
\itemize{
\item \code{p_value}: fraction of simulated null draws larger than \code{test_score}.
\item \code{test_score}: the observed statistic multiplied by \code{nrow(tau_matrix)},
matching the paper's asymptotic null scale.
\item \code{eigenvalues}: nonnegative eigenvalues of the empirical covariance matrix
used as weights in the simulated null distribution.
}

The three entries are returned together because they answer different
diagnostic questions. \code{p_value} is the value used by \code{\link[=fssd_rand_test]{fssd_rand_test()}} and
\code{\link[=fssd_opt_test]{fssd_opt_test()}} to make the hypothesis-test object. \code{test_score} records
the exact scaled number compared with the null draws, which avoids ambiguity
about whether the unscaled or \code{n}-scaled statistic was used. \code{eigenvalues}
expose the estimated null distribution; if most eigenvalues are numerically
zero, the finite feature set or sample size may be giving a nearly
degenerate null simulation.
}
\description{
Simulates the asymptotic null distribution used to turn an observed FSSD
statistic into a p-value.
}
\details{
Let \verb{lambda_1, ..., lambda_m} be the eigenvalues of the centered empirical
feature covariance matrix
\deqn{\hat\Sigma = n^{-1}\sum_i(\tau_i-\bar\tau)(\tau_i-\bar\tau)^T.}
The null draws have the form
\deqn{\sum_j \lambda_j (Z_j^2 - 1),}
with independent standard normal \code{Z_j}. The observed value compared against
those draws is \code{n * fssd_stat}. This matches the FSSD paper's null result for
the scaled statistic \code{n * FSSDhat^2}, using the empirical plug-in covariance
of \code{tau(X)} in place of the population covariance.

This is the final lower-level step used by \code{\link[=fssd_rand_test]{fssd_rand_test()}} and
\code{\link[=fssd_opt_test]{fssd_opt_test()}}. It is exported so users can change the number of null
simulations, inspect the eigenvalues, or compare the same observed statistic
under different Monte Carlo seeds without recomputing \code{tau}.
}
\examples{
tau <- matrix(c(1, 2, 3), ncol = 1)
compute_fssd_null_pvalue(tau, fssd_stat = 0.1, n_simulations = 10)
}
