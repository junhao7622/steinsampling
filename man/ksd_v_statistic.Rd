% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ksd_v_test.R
\name{ksd_v_statistic}
\alias{ksd_v_statistic}
\title{Compute the KSD V-statistic from its Stein-kernel matrix}
\usage{
ksd_v_statistic(U_mat)
}
\arguments{
\item{U_mat}{Numeric square matrix of pairwise Stein kernel values.}
}
\value{
One numeric value: the scaled V-statistic \verb{n V_n}. This is the same scale as
the values returned by \code{\link[=ksd_v_bootstrap]{ksd_v_bootstrap()}} and as the \code{statistic} component
of \code{\link[=ksd_v_test]{ksd_v_test()}}. It is returned as a plain number because the kernel choice
and wild-bootstrap settings are handled by the higher-level test object.
}
\description{
The returned statistic is \code{n * mean(U_mat)}, which is the same as summing all
entries of the matrix and dividing by \code{n}.
}
\details{
If \code{U_mat[i, j] = k0(x_i, x_j)}, the statistic is
\deqn{\frac{1}{n} \sum_{i=1}^n \sum_{j=1}^n U_{ij}.}
This is the scaled statistic \verb{n V_n} when
\eqn{V_n = n^{-2} \sum_{i,j} U_{ij}}.
The helper is separated out so users can reuse a precomputed Stein-kernel
matrix or compare dense and blocked matrix calculations. It is the
matrix-compression layer between \code{\link[=ksd_vq_matrix]{ksd_vq_matrix()}}, which evaluates scores
and kernels, and \code{\link[=ksd_v_bootstrap]{ksd_v_bootstrap()}}, which keeps the matrix fixed and
changes only the wild-bootstrap sign sequences.

The diagonal is included here because the V-statistic treats every ordered
pair of rows, including \verb{(i, i)}, as part of the empirical average. This is
the main algebraic difference from \code{\link[=ksd_u_statistic]{ksd_u_statistic()}}. Keeping the two
compression functions separate prevents accidental mixing of U-statistic and
V-statistic conventions when users inspect lower-level matrices.

This function performs no bootstrap and does not evaluate the score
function. It is the deterministic compression from the full pairwise matrix
to the one-number V-statistic used by \code{\link[=ksd_v_test]{ksd_v_test()}}.

Because the function only needs \code{U_mat}, it is useful for checking whether
two ways of building the same Stein-kernel matrix, for example dense and
blocked computation, lead to the same final statistic before any bootstrap
randomness is introduced.
}
\examples{
U <- matrix(c(1, 0.2, 0.3, 0.2, 1, 0.4, 0.3, 0.4, 1), 3, 3)
ksd_v_statistic(U)
}
