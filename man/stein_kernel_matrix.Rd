% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kernel_classes.R
\name{stein_kernel_matrix}
\alias{stein_kernel_matrix}
\title{Compute pairwise Stein kernel values}
\usage{
stein_kernel_matrix(kernel, X, grads, Y = NULL, grads_Y = NULL, ...)
}
\arguments{
\item{kernel}{A Stein kernel object.}

\item{X}{Numeric matrix with samples in rows.}

\item{grads}{Score matrix for \code{X}.}

\item{Y}{Optional second sample matrix. If \code{NULL}, \code{Y = X}.}

\item{grads_Y}{Optional score matrix for \code{Y}. Required when \code{Y} is supplied.}

\item{...}{Additional arguments passed to kernel methods.}
}
\value{
Numeric matrix. If \code{Y = NULL}, the matrix is \verb{nrow(X) x nrow(X)} and entry
\verb{(i, j)} is \code{k0(X[i, ], X[j, ])}. If \code{Y} is supplied, the matrix is
\verb{nrow(X) x nrow(Y)} and entry \verb{(i, j)} is \code{k0(X[i, ], Y[j, ])}. The return
value is a matrix rather than a scalar because the algorithms use the same
pairwise object in different ways: KSD averages it, Stein Points and SP-MCMC
accumulate it while constructing support points, and Stein thinning uses it
to compress an existing sample by choosing row indices.
}
\description{
Combines a base kernel, its derivatives, and the target scores to form the
Stein kernel \code{k0}. This is the common matrix object used by the package's KSD
tests, FSSD features, Stein thinning, Stein Points, and SP-MCMC.
}
\details{
Liu, Lee, and Jordan write this object as \verb{u_q(x, x')} because their target
density is denoted by \code{q}. This manual uses \code{p} for the target density and
the shorter name \code{k0(x, y)} for the same Stein-kernel quantity, because it is
also used outside KSD tests, for example in Stein thinning and Stein Points.
If \verb{s_p(x) = \\nabla_x \\log p(x)} is the target score and \code{k(x, y)} is the
base kernel, the returned matrix has entries
\deqn{
k0(x, y) =
s_p(x)^T s_p(y) k(x, y)
+ s_p(x)^T \nabla_y k(x, y)
+ s_p(y)^T \nabla_x k(x, y)
+ \nabla_x \cdot \nabla_y k(x, y).
}
The first term couples the two score vectors through the base kernel. The
middle two terms couple one score vector with the kernel derivative in the
other argument. The last term is the trace of the mixed derivative matrix.
These are the four terms that appear after applying the Langevin Stein
operator in each argument of the base kernel.

To compute this matrix the caller must provide points and their scores in
matching row order. \code{X[i, ]} is paired with \code{grads[i, ]}, and, when \code{Y} is
supplied, \code{Y[j, ]} is paired with \code{grads_Y[j, ]}. If \code{Y} is omitted, the same
rows and scores are used on both sides. The function never estimates scores
by itself; top-level functions such as \code{\link[=ksd_u_test]{ksd_u_test()}} call the user's
\code{score_function} first and then pass the checked score matrix down to this
kernel layer.

In KSD tests, the entries of this matrix are averaged to form the test
statistic. In Stein Points and SP-MCMC, the same entries define the objective
for constructing a new support point. In Stein thinning, they are used
differently: the candidate rows are already fixed by an existing sample, and
\code{k0} scores which row index should be kept next in the compressed empirical
measure. In FSSD, the same Stein operator is evaluated against finite test
locations through \code{\link[=compute_tau]{compute_tau()}}. Supplying \code{Y} computes the rectangular
matrix between rows of \code{X} and rows of \code{Y}; omitting \code{Y} computes the square
matrix for \code{X}.

This function is the central kernel layer of the package. The higher-level
routines do not each reimplement the Stein formula; they all call this
function, or the lower-level generics used by it. This design keeps the
mathematical definition of \code{k0} in one place and lets a custom kernel work
across tests, point selection, thinning, and SVGD-style calculations.
Gaussian RBF and position-distance IMQ objects use fused methods for this
same formula. Objects from \code{\link[=custom_stein_kernel]{custom_stein_kernel()}} use the default method,
which assembles \code{k0} from their three leaf functions and therefore works
directly when the base kernel depends only on \verb{(X, Y)}. A target-dependent
base kernel whose value or derivatives also require the scores cannot use
that wrapper: its class must provide a complete
\verb{stein_kernel_matrix.<class>()} method. \code{\link[=stein_kernel_imq_score]{stein_kernel_imq_score()}} is the
built-in example of this score-dependent route.

Moving sideways from this function to \code{\link[=compute_tau]{compute_tau()}} changes the object
being stored. \code{stein_kernel_matrix()} stores scalar pairwise values
\code{k0(x_i, y_j)}. \code{\link[=compute_tau]{compute_tau()}} stores the finite-dimensional Stein feature
vectors \verb{s_p(x_i) k(x_i, v_j) + \\nabla_x k(x_i, v_j)} used by FSSD before
taking inner products. Both functions use the same base-kernel methods and
the same score convention.
}
\examples{
X <- matrix(c(-1, 0, 1), ncol = 1)
grads <- -X
kernel <- stein_kernel(type = "gaussian_rbf", h = 1)
stein_kernel_matrix(kernel, X, grads)
}
