% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fssd_test.R
\name{compute_fssd_unbiased_stat}
\alias{compute_fssd_unbiased_stat}
\title{Estimate squared FSSD from a feature matrix}
\usage{
compute_fssd_unbiased_stat(tau_matrix)
}
\arguments{
\item{tau_matrix}{Numeric FSSD feature matrix.}
}
\value{
One numeric value: the unbiased estimate of squared FSSD for the supplied
feature matrix. This is the value stored as \code{statistic} by \code{\link[=fssd_rand_test]{fssd_rand_test()}}
and \code{\link[=fssd_opt_test]{fssd_opt_test()}} before it is scaled by \code{n} for the null comparison. It
is returned as a plain number because the location matrix \code{V}, kernel choice,
and null-simulation settings are carried by the higher-level test object.
}
\description{
Computes the unbiased U-statistic estimate of squared FSSD from the feature
matrix returned by \code{\link[=compute_tau]{compute_tau()}}.
}
\details{
If \code{tau_i} is row \code{i} of \code{tau_matrix}, the returned value is
\deqn{
\frac{1}{n(n - 1)} \sum_{i \ne j} \tau_i^T \tau_j.
}
This is the sample version of the paper's \code{FSSD^2} for the selected test
locations. The implementation uses column sums to avoid explicitly forming
all pairwise inner products.

This function is separated from \code{\link[=compute_tau]{compute_tau()}} because the same feature
matrix is also needed for the null simulation. Keeping the statistic as its
own step makes the FSSD workflow explicit: features first, U-statistic
second, null simulation third.

The diagonal terms \verb{tau_i^T tau_i} are excluded for the same reason that a
U-statistic excludes self-pairs: the target quantity is an expectation over
two independent draws from the sample distribution. The implementation uses
the identity
\deqn{
\sum_{i \ne j} \tau_i^T \tau_j =
||\sum_i \tau_i||^2 - \sum_i ||\tau_i||^2
}
so it can compute the statistic from column sums and row norms rather than
forming the full \verb{n x n} matrix of feature inner products.
}
\examples{
tau <- matrix(c(1, 2, 3), ncol = 1)
compute_fssd_unbiased_stat(tau)
}
