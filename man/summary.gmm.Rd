% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gmm_model.R
\name{summary.gmm}
\alias{summary.gmm}
\alias{print.summary.gmm}
\title{Summarize a Gaussian mixture model}
\usage{
\method{summary}{gmm}(object, ...)

\method{print}{summary.gmm}(x, ...)
}
\arguments{
\item{object}{A \code{"gmm"} object.}

\item{...}{Ignored.}

\item{x}{A \code{"summary.gmm"} object.}
}
\value{
\code{summary()} returns an object of class \code{"summary.gmm"} with components
\code{nComp}, \code{d}, and \code{components}. \code{print()} renders it and
returns \code{x} invisibly.
}
\description{
Lays out one row per mixture component with its weight, mean, and marginal
standard deviations.
}
\details{
\code{components} has one row per component: the weight \eqn{w_k}, the entries of
\eqn{\mu_k}, and \eqn{\sqrt{\mathrm{diag}(\Sigma_k)}}. Off-diagonal
covariance entries are not shown; read them from \code{object$sigma} when the
components are correlated.
}
\examples{
model <- gmm(nComp = 2, mu = cbind(c(-2, 0), c(2, 0)),
             sigma = array(diag(2), c(2, 2, 2)), weights = c(0.3, 0.7),
             d = 2)
summary(model)
}
\seealso{
\code{\link[=gmm]{gmm()}}
}
