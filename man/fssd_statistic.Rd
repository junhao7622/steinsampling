% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fssd_test.R
\name{fssd_statistic}
\alias{fssd_statistic}
\title{Compute the scaled FSSD test statistic}
\usage{
fssd_statistic(tau_matrix)
}
\arguments{
\item{tau_matrix}{Numeric \eqn{\tilde n\times dJ} feature matrix returned by
\code{\link[=compute_tau]{compute_tau()}}.}
}
\value{
One numeric value: \eqn{S_{\tilde n}=\tilde n\widehat{FSSD}^2}.
}
\description{
Computes the scaled off-diagonal FSSD U-statistic from a feature matrix
returned by \code{\link[=compute_tau]{compute_tau()}}.
}
\details{
If \code{tau_i} is row \code{i} of \code{tau_matrix} and \eqn{\tilde n} is its number of
rows, the returned value is
\deqn{
S_{\tilde n}
  = \tilde n\widehat{FSSD}^2
  = \frac{1}{\tilde n - 1}\sum_{i\ne j}\tau_i^T\tau_j.
}
Diagonal terms are excluded because population FSSD uses two independent
draws. Although population \code{FSSD^2} is nonnegative, its unbiased
off-diagonal estimator and the returned \eqn{S_{\tilde n}} can be negative
in finite samples. This function does not compute a p-value.
}
\examples{
tau <- matrix(c(1, 2, 3), ncol = 1)
fssd_statistic(tau)
}
