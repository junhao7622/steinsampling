% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/svgd.R
\name{update_svgd}
\alias{update_svgd}
\title{Run SVGD particle updates}
\usage{
update_svgd(
  object,
  x0,
  lnprob,
  n_iter = 1000,
  stepsize = 0.001,
  kernel = NULL,
  alpha = 0.9,
  adj_grad = NULL,
  trace_iters = NULL
)
}
\arguments{
\item{object}{SVGD object created by \code{\link[=svgd]{svgd()}}.}

\item{x0}{Numeric matrix of initial particles. A vector is treated as
one-dimensional particles.}

\item{lnprob}{Function returning the target score at the current particles,
despite the historical name. It should return
\eqn{\nabla_x \log p(x)}, not \verb{log p(x)}, with the same dimensions as \code{x0}.}

\item{n_iter}{Number of SVGD iterations.}

\item{stepsize}{SVGD step size. Custom \code{adj_grad} functions should not
multiply by this value; it is applied by \code{update_svgd()}.}

\item{kernel}{Optional \code{SteinKernel} overriding \code{object$kernel}.}

\item{alpha}{Exponential-decay/momentum coefficient used by the package's
default RMSProp-style scaling. It is an implementation setting, not a
parameter of the raw direction in Algorithm 1.}

\item{adj_grad}{Optional function for custom scaling of the SVGD direction.
It receives the arguments documented for \code{\link[=custom_adjusted_gradient]{custom_adjusted_gradient()}} and
must return a matrix with the same dimensions as \code{theta}.}

\item{trace_iters}{Optional integer vector of iterations to save. If \code{NULL},
return only the final particles.}
}
\value{
If \code{trace_iters = NULL}, an \verb{n x d} numeric matrix containing the final
particle locations after \code{n_iter} updates. If \code{trace_iters} is supplied, a
list with:
\itemize{
\item \code{theta}: final \verb{n x d} particle matrix.
\item \code{trace}: list of saved particle matrices; the element names are the
iteration numbers requested in \code{trace_iters}.
}

The default return is only the final matrix because SVGD is usually used as
an iterative transport map from initial particles to final particles. The
optional trace is returned only when requested because storing every particle
matrix can be large. Keeping the final matrix in \code{theta} and the selected
intermediate matrices in \code{trace} makes the two use cases explicit: downstream
algorithms normally use the final particles, while diagnostics and plots use
the saved trajectory.
}
\description{
Applies the SVGD update repeatedly to a particle matrix. The target enters
only through \code{lnprob}, which should return the score at each particle.
}
\details{
The name \code{lnprob} is kept for compatibility with common SVGD code, but the
function must return the score \eqn{\nabla_x \log p(x)}, not the scalar log
density. Rows of \code{theta} are particles and columns are coordinates, so
\code{lnprob(theta)} should return an \verb{n x d} matrix with the same shape.

At each iteration the particles \code{theta} are moved by
\deqn{\theta \leftarrow \theta + \mathrm{stepsize} \times \mathrm{direction}.}
Before the optional adaptive scaling, the raw SVGD direction for
particle \code{theta_i} is the empirical version of the paper's optimal Stein
direction:
\deqn{
g(\theta_i) =
\frac{1}{n} \sum_{j=1}^n
\left[
k(\theta_j, \theta_i) s_p(\theta_j)
+ \nabla_{\theta_j} k(\theta_j, \theta_i)
\right].
}
Here the score at particle \code{j} is returned by \code{lnprob}. The first term moves
particles toward regions where the target density is high, smoothed through
the kernel. The derivative term acts as a repulsive force, keeping particles
from collapsing to the same mode. This raw direction is the quantity specified
by Algorithm 1. That algorithm leaves the iteration step size
\eqn{\epsilon_l} unspecified; the experiments state that AdaGrad is used,
and the Bayesian neural-network experiment refers to AdaGrad with momentum,
but the paper does not give the coordinatewise recurrence implemented here.
Thus the default scaling below is an experimental/implementation-level choice,
not part of the analytic definition of the SVGD direction. In implementation
terms, \code{kxy \%*\% lnpgrad} is the matrix form of
\eqn{\sum_j k(\theta_j,\theta_i)s_p(\theta_j)}, and \code{dxkxy} stores
\eqn{\sum_j \nabla_{\theta_j}k(\theta_j,\theta_i)} for each destination
particle \code{i}.

Concretely, if \code{g_l} is the raw direction, the default keeps the exponential
moving average
\deqn{H_1=g_1^2,\qquad
H_l=\alpha H_{l-1}+(1-\alpha)g_l^2}
coordinatewise and uses
\eqn{g_l/(\delta+\sqrt{H_l})}, with
\eqn{\delta=10^{-6}}, before multiplying by the fixed outer \code{stepsize}.
This is closer to RMSProp, or to an AdaGrad-with-momentum implementation,
than to classical cumulative-sum AdaGrad. To apply Algorithm 1 without this
package scaling, pass an identity adjustment such as
\code{adj_grad = function(grad, ...) grad}. A custom \code{adj_grad} may otherwise
replace only the scaling step. If \code{trace_iters} is supplied, copies of the
particle matrix are saved after those iterations.
}
\examples{
obj <- svgd()
x0 <- matrix(seq(-2, 2, length.out = 5), ncol = 1)
target_score <- function(theta) -theta
update_svgd(obj, x0, target_score, n_iter = 5, stepsize = 0.05)
}
