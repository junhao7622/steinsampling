% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/svgd.R
\name{update_svgd}
\alias{update_svgd}
\title{Run Stein Variational Gradient Descent updates.}
\usage{
update_svgd(
  object,
  x0,
  lnprob,
  n_iter = 1000,
  stepsize = 0.001,
  kernel = NULL,
  alpha = 0.9,
  adj_grad = NULL,
  trace_iters = NULL
)
}
\arguments{
\item{object}{SVGD object created by \code{\link[=svgd]{svgd()}}.}

\item{x0}{Numeric matrix of initial particles (\code{n} x \code{d}). A vector is
treated as one-dimensional particles.}

\item{lnprob}{Function returning the score \verb{nabla_x log p(x)} evaluated at
the current particles. It must return an object with the same dimensions
as \code{x0}.}

\item{n_iter}{Number of SVGD iterations.}

\item{stepsize}{Outer SVGD step size. Do not multiply by this inside a custom
\code{adj_grad}; the update always uses \code{theta <- theta + stepsize * direction}.}

\item{kernel}{Optional \code{SteinKernel} overriding \code{object$kernel}.}

\item{alpha}{Momentum parameter for the running average of squared SVGD
directions used by the default AdaGrad adjustment.}

\item{adj_grad}{Optional function defining a custom adjusted-gradient rule.
If \code{NULL}, use the default AdaGrad rule
\code{grad / (fudge_factor + sqrt(historical_grad))}. If supplied, the function
is called by \code{\link[=custom_adjusted_gradient]{custom_adjusted_gradient()}} with named arguments \code{grad},
\code{historical_grad}, \code{iter}, \code{theta}, \code{stepsize}, \code{alpha}, and
\code{fudge_factor}, and must return an update direction with the same
dimensions as \code{theta}.}

\item{trace_iters}{Optional integer vector of iterations to save. If \code{NULL},
return only the final particles.}
}
\value{
If \code{trace_iters} is \code{NULL}, a numeric matrix of final particles.
Otherwise a list with \code{theta} and named \code{trace} entries.
}
\description{
Applies SVGD to a set of particles. By default the update direction is
adjusted with the AdaGrad-with-momentum rule used in the SVGD experiments.
Passing \code{adj_grad} replaces only this adjusted-gradient calculation; the
SVGD direction, bandwidth, and history are still computed inside this
function.
}
\examples{
obj <- svgd()
x0 <- matrix(rnorm(5), ncol = 1)
score <- function(theta) -theta
update_svgd(obj, x0, score, n_iter = 2, stepsize = 0.01)
}
