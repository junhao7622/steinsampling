% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kernel_classes.R
\name{stein_kernel}
\alias{stein_kernel}
\title{Create a built-in Stein kernel}
\usage{
stein_kernel(
  type = c("gaussian_rbf", "imq"),
  sigma = NULL,
  h = NULL,
  beta = -0.5,
  c = 1,
  precon = NULL
)
}
\arguments{
\item{type}{Kernel type, \code{"gaussian_rbf"} or \code{"imq"}.}

\item{sigma, h}{Gaussian RBF bandwidth. Provide at most one.}

\item{beta}{IMQ exponent.}

\item{c}{IMQ length scale.}

\item{precon}{Optional positive definite matrix used to rescale distances.}
}
\value{
A list with class \code{"SteinKernel_gaussian_rbf"} or \code{"SteinKernel_imq"} plus
the common class \code{"SteinKernel"}. For Gaussian RBF kernels it stores \code{type},
optional squared bandwidth \code{h2}, and optional preconditioner \code{precon}. If
\code{h2} is \code{NULL}, functions that need a concrete RBF scale choose one from the
data by the median squared-distance rule. For IMQ kernels it stores \code{type},
exponent \code{beta}, length scale \code{c}, and optional \code{precon}. The object is meant
to be passed to functions such as
\code{\link[=stein_kernel_matrix]{stein_kernel_matrix()}}, \code{\link[=ksd_u_test]{ksd_u_test()}}, \code{\link[=fssd_test]{fssd_test()}}, \code{\link[=stein_points]{stein_points()}},
\code{\link[=sp_mcmc]{sp_mcmc()}}, and \code{\link[=stein_thinning]{stein_thinning()}}.
}
\description{
Creates one of the built-in base kernels and stores the parameters needed to
assemble the Stein kernel \code{k0}. The returned object can be passed to KSD,
FSSD, SVGD, Stein Points, SP-MCMC, and Stein thinning functions.
}
\details{
The Gaussian RBF base kernel is
\deqn{k(x, y) = \exp(-r(x, y) / (2 h^2)),}
where \code{h} is the bandwidth and \code{h^2} is the squared scale stored in the
object. The IMQ base kernel is
\deqn{k(x, y) = (c^2 + r(x, y))^\beta,}
where \code{c} is a positive length scale, \code{c^2} is the squared offset, and
\code{beta} is negative. The two choices are parallel inputs to the same Stein
kernel machinery: after the object is built, \code{\link[=eval_kernel]{eval_kernel()}},
\code{\link[=grad_x_kernel]{grad_x_kernel()}}, \code{\link[=trace_mixed_kernel]{trace_mixed_kernel()}}, \code{\link[=cross_kernel]{cross_kernel()}}, and
\code{\link[=stein_kernel_matrix]{stein_kernel_matrix()}} use dispatch to obtain the correct formulas.

The Gaussian RBF is common in KSD, FSSD, and SVGD examples. Its values decay
exponentially with the squared distance, so too small a bandwidth can make
off-diagonal entries nearly zero. The IMQ kernel has polynomial decay because
\code{beta < 0}; it is the default for Stein thinning and is often used in Stein
Points and SP-MCMC because the Stein Points and thinning papers study its
convergence properties. Moving sideways from RBF to IMQ usually means keeping
the same \code{X}, score function, and top-level algorithm, but replacing the
scale parameter \code{h^2} by the offset \code{c^2} and choosing a negative \code{beta}.

By default,
\deqn{r(x, y) = ||x - y||^2.}
If \code{precon} is supplied, distances become
\deqn{r(x, y) = (x - y)^T precon (x - y).}
The same \code{r(x, y)} is used in the base kernel, first derivatives, and mixed
trace terms. The top-level KSD and FSSD functions pass \code{scaling} as a
squared scale: for RBF, \code{h^2 = scaling}; for IMQ, \code{c^2 = scaling}. When
\code{scaling = NULL}, \code{\link[=find_median_distance]{find_median_distance()}} supplies the median pairwise
squared distance on that same scale.

The function returns a small S3 object rather than a closure because the
package needs several related operations from the same kernel: the base
kernel, its first derivative, the mixed-derivative trace, and the FSSD
optimization derivative. Storing parameters in an object lets
\code{\link[=stein_kernel_matrix]{stein_kernel_matrix()}}, \code{\link[=compute_tau]{compute_tau()}}, and the kernel generics dispatch to
consistent methods.

For Stein Points experiments there are two additional kernel constructors:
\code{\link[=stein_kernel_inverse_log]{stein_kernel_inverse_log()}} for a position-distance inverse-log kernel and
\code{\link[=stein_kernel_imq_score]{stein_kernel_imq_score()}} for an IMQ kernel on score-vector distances. Those
objects live at the same kernel layer as the objects created here, but they
are specialized enough that they have separate constructors.
}
\examples{
stein_kernel(type = "gaussian_rbf", h = 1)
stein_kernel(type = "imq", c = 1, beta = -0.5)
}
