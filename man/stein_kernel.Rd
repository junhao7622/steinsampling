% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kernel_classes.R
\name{stein_kernel}
\alias{stein_kernel}
\title{Create a built-in Stein kernel}
\usage{
stein_kernel(
  type = c("gaussian_rbf", "imq"),
  sigma = NULL,
  h = NULL,
  beta = -0.5,
  c = 1,
  precon = NULL
)
}
\arguments{
\item{type}{Kernel type, \code{"gaussian_rbf"} or \code{"imq"}.}

\item{sigma, h}{Gaussian RBF bandwidth. Provide at most one.}

\item{beta}{IMQ exponent.}

\item{c}{IMQ length scale.}

\item{precon}{Optional positive definite matrix used to rescale distances.}
}
\value{
A list with class \code{"SteinKernel_gaussian_rbf"} or \code{"SteinKernel_imq"} plus
the common class \code{"SteinKernel"}. For Gaussian RBF kernels it stores \code{type},
optional squared bandwidth \code{h2}, and optional \code{precon}; \code{h2 = NULL} invokes
the median squared-distance rule where supported. IMQ objects store \code{type},
\code{beta}, \code{c}, and optional \code{precon}. Pass the result to
\code{\link[=stein_kernel_matrix]{stein_kernel_matrix()}}, \code{\link[=ksd_u_test]{ksd_u_test()}}, \code{\link[=fssd_test]{fssd_test()}}, \code{\link[=stein_points]{stein_points()}},
\code{\link[=sp_mcmc]{sp_mcmc()}}, and \code{\link[=stein_thinning]{stein_thinning()}}.
For adaptive RBF, KSD/FSSD freeze the input-data median scale, whereas SVGD
recomputes the paper's dynamic median each iteration. Stein Points, SP-MCMC,
coordinate descent, and Stein thinning require explicit fixed \code{h} because
their KSD objectives need one kernel for all candidate and diagonal values.
}
\description{
Creates one of the built-in base kernels and stores the parameters needed to
assemble the Stein kernel \code{k0}. The returned object can be passed to KSD,
FSSD, SVGD, Stein Points, SP-MCMC, and Stein thinning functions.
}
\details{
The Gaussian RBF base kernel is
\deqn{k(x, y) = \exp(-r(x, y) / (2 h^2)),}
where \code{h} is the bandwidth and \code{h^2} is the squared scale stored in the
object. The IMQ base kernel is
\deqn{k(x, y) = (c^2 + r(x, y))^\beta,}
where positive \code{c} is the length scale, \code{c^2} the squared offset, and \code{beta}
is negative. Both feed the same Stein-kernel machinery: \code{\link[=eval_kernel]{eval_kernel()}},
\code{\link[=grad_x_kernel]{grad_x_kernel()}}, \code{\link[=trace_mixed_kernel]{trace_mixed_kernel()}}, \code{\link[=cross_kernel]{cross_kernel()}}, and
\code{\link[=stein_kernel_matrix]{stein_kernel_matrix()}} dispatch to the corresponding formulas.

RBF is common in KSD, FSSD, and SVGD; exponential squared-distance decay can
make off-diagonal entries nearly zero when \code{h} is too small. IMQ has
polynomial decay for \code{beta < 0}, is the Stein-thinning default, and is common
in Stein Points/SP-MCMC because their papers study its convergence. Switching
from RBF to IMQ keeps \code{X}, score, and algorithm but replaces \code{h^2} by \code{c^2}
and selects negative \code{beta}.

By default,
\deqn{r(x, y) = ||x - y||^2.}
If \code{precon} is supplied, distances become
\deqn{r(x, y) = (x - y)^T precon (x - y).}
The base kernel, first derivatives, and mixed trace all use this \code{r}.
Top-level KSD/FSSD pass squared \code{scaling}: \code{h^2} for RBF and \code{c^2} for IMQ.
With \code{scaling = NULL}, \code{\link[=find_median_distance]{find_median_distance()}} returns the squared exact
all-pair median; its quadratic time and memory cost favors fixed scales for
large samples.

An S3 object stores parameters so the base kernel, first derivative, mixed
trace, and FSSD optimization derivative dispatch consistently through
\code{\link[=stein_kernel_matrix]{stein_kernel_matrix()}}, \code{\link[=compute_tau]{compute_tau()}}, and the kernel generics.

Specialized Stein Points constructors at this layer are
\code{\link[=stein_kernel_inverse_log]{stein_kernel_inverse_log()}} for position-distance inverse-log and
\code{\link[=stein_kernel_imq_score]{stein_kernel_imq_score()}} for score-distance IMQ.
}
\examples{
stein_kernel(type = "gaussian_rbf", h = 1)
stein_kernel(type = "imq", c = 1, beta = -0.5)
}
