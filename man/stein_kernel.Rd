% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kernel_classes.R
\name{stein_kernel}
\alias{stein_kernel}
\title{Create a built-in Stein kernel}
\usage{
stein_kernel(
  type = c("gaussian_rbf", "imq"),
  sigma = NULL,
  h = NULL,
  beta = -0.5,
  c = 1,
  precon = NULL
)
}
\arguments{
\item{type}{\code{"gaussian_rbf"} or \code{"imq"}.}

\item{sigma, h}{Gaussian RBF bandwidth \eqn{h>0}. Supply at most one.}

\item{beta}{Finite IMQ exponent \eqn{\beta<0}.}

\item{c}{Positive IMQ length scale \eqn{c}.}

\item{precon}{Optional symmetric positive-definite \eqn{M}.}
}
\value{
A \code{SteinKernel} object.
}
\description{
Create a built-in Stein kernel
}
\details{
With \eqn{r_M(x,y)=(x-y)^\top M(x-y)} and \code{M = precon} (identity when
\code{precon = NULL}), the base kernels are
\deqn{k_{\mathrm{RBF}}(x,y)=\exp\{-r_M(x,y)/(2h^2)\},\qquad
      k_{\mathrm{IMQ}}(x,y)=\{c^2+r_M(x,y)\}^{\beta}.}
Leaving \code{h} and \code{sigma} unset gives a Gaussian RBF whose bandwidth is the
median-distance heuristic recomputed from the points at each call. KSD and
FSSD accept that; the sequential algorithms require a fixed \code{h}.
}
\examples{
stein_kernel("gaussian_rbf", h = 1)
stein_kernel("imq", c = 1, beta = -0.5)
}
