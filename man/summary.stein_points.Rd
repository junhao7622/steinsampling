% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stein_points.R
\name{summary.stein_points}
\alias{summary.stein_points}
\alias{summary.stein_codescent}
\alias{print.summary.stein_points}
\title{Summarize a Stein point set}
\usage{
\method{summary}{stein_points}(object, ...)

\method{summary}{stein_codescent}(object, ...)

\method{print}{summary.stein_points}(x, ...)
}
\arguments{
\item{object}{A \code{"stein_points"} or \code{"stein_codescent"} object.}

\item{...}{Ignored.}

\item{x}{A \code{"summary.stein_points"} object.}
}
\value{
A \code{"summary.stein_points"} object with components \code{method}, \code{kernel}, \code{n},
\code{d}, \code{evaluation_label}, \code{n_eval_total}, and \code{call}. Stein Points adds
\code{ksd_last} and \code{truncation}, plus \code{c2} when truncation is enabled.
Coordinate descent adds \code{n_iter}.
The print method returns \code{x} invisibly.
}
\description{
Summarizes the point-set size, kernel, method-specific settings, and
evaluation cost for \code{\link[=stein_points]{stein_points()}} or \code{\link[=stein_codescent]{stein_codescent()}}.
}
\details{
\code{evaluation_label} names what \code{n_eval_total} counts for this class.
Coordinate-descent objectives are not displayed because they change with
the row being updated and are not point-set KSDs.
}
\examples{
score_function <- function(x) -as.matrix(x)
kernel <- stein_kernel(type = "gaussian_rbf", h = 1)
fit <- stein_points(score_function, kernel, n_points = 5, d = 1,
                    optimizer = fmin_grid(lb = -3, ub = 3, n0 = 20),
                    log_p = function(x) -0.5 * rowSums(as.matrix(x)^2))
summary(fit)
}
\seealso{
\code{\link[=print.stein_points]{print.stein_points()}}
}
