% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/svgd.R
\name{custom_adjusted_gradient}
\alias{custom_adjusted_gradient}
\title{Apply a custom SVGD direction adjustment}
\usage{
custom_adjusted_gradient(
  adj_grad,
  grad,
  historical_grad,
  iter,
  theta,
  stepsize,
  alpha,
  fudge_factor
)
}
\arguments{
\item{adj_grad}{User function that returns the adjusted update direction.}

\item{grad}{Current raw SVGD direction.}

\item{historical_grad}{Exponential moving average of \code{grad^2}, maintained by
\code{\link[=update_svgd]{update_svgd()}}.}

\item{iter}{Current iteration index.}

\item{theta}{Current particle matrix.}

\item{stepsize}{SVGD step size.}

\item{alpha}{Exponential-decay/momentum coefficient used to update
\code{historical_grad}.}

\item{fudge_factor}{Small numerical stabilizer used by the default
RMSProp-style rule.}
}
\value{
Numeric matrix with the same shape as \code{grad} and \code{theta}. The matrix is the
adjusted direction; \code{\link[=update_svgd]{update_svgd()}} multiplies it by \code{stepsize} and adds it
to the particles.
}
\description{
Calls a user-supplied adjustment function for the scaling part of an SVGD
update. It receives the raw SVGD direction and the running squared-gradient
history, then returns the direction that \code{\link[=update_svgd]{update_svgd()}} will multiply by the
step size.
}
\details{
SVGD itself determines the raw direction \code{grad}. This helper deliberately
touches only the rescaling step. Custom adjustment functions are useful when
the raw direction should be rescaled in a way other than the package's default
RMSProp-style rule. The returned matrix is multiplied by \code{stepsize} later
inside \code{\link[=update_svgd]{update_svgd()}}.

This wrapper exists so custom adjustment functions receive a stable set of
arguments. It keeps user code from depending on local variable names inside
\code{\link[=update_svgd]{update_svgd()}}, while making clear that custom adjustments should return a
direction, not a fully stepped particle matrix.
}
\examples{
adj <- function(grad, historical_grad, ...) grad / (1 + sqrt(historical_grad))
custom_adjusted_gradient(
  adj, grad = matrix(1), historical_grad = matrix(1),
  iter = 1, theta = matrix(0), stepsize = 0.1,
  alpha = 0.9, fudge_factor = 1e-6
)
}
