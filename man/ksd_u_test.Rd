% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ksd_u_test.R
\name{ksd_u_test}
\alias{ksd_u_test}
\title{KSD goodness-of-fit test using the Liu et al. U-statistic}
\usage{
ksd_u_test(
  X,
  score_function,
  boot_method = "multinomial_centered",
  scaling = NULL,
  nboot = 1000,
  kernel = c("gaussian_rbf", "imq"),
  return_raw_boot = FALSE,
  median_max_samples = 2000,
  median_use_sampling = TRUE,
  median_seed = 123,
  block_size = NULL,
  block_threshold = 5000,
  imq_beta = -0.5
)
}
\arguments{
\item{X}{Numeric vector or matrix of samples. A vector is treated as \verb{n x 1}.}

\item{score_function}{Function returning the target score for each row of
\code{X}.}

\item{boot_method}{Bootstrap method. Currently only
\code{"multinomial_centered"} is supported.}

\item{scaling}{Positive kernel scale. \code{NULL} uses the median squared
distance between sample pairs.}

\item{nboot}{Number of bootstrap samples.}

\item{kernel}{Kernel choice: \code{"gaussian_rbf"}, \code{"imq"}, or a \code{SteinKernel}
object, including objects created by \code{\link[=custom_stein_kernel]{custom_stein_kernel()}}.}

\item{return_raw_boot}{Logical; if \code{TRUE}, return bootstrap samples.}

\item{median_max_samples, median_use_sampling, median_seed}{Options used by
the median-scale rule when \code{scaling = NULL}.}

\item{block_size, block_threshold}{Options for computing large kernel
matrices in blocks.}

\item{imq_beta}{IMQ kernel exponent.}
}
\value{
An object of class \code{htest}, implemented as a list with:
\itemize{
\item \code{statistic}: one number labeled \code{ksd_u}; this is the observed
off-diagonal U-statistic.
\item \code{p.value}: right-tail bootstrap p-value using the package's
\code{(1 + exceedances) / (nboot + 1)} finite-bootstrap convention.
\item \code{method}: text naming the KSD U-statistic test and kernel.
\item \code{data.name}: expression used for \code{X}.
\item \code{parameter}: numeric vector containing \code{nboot}, the squared kernel
\code{scaling} used, and \code{imq_beta} when the IMQ kernel is used.
\item \code{bootstrap_samples}: present only when \code{return_raw_boot = TRUE}; numeric
vector of bootstrap U-statistics on the same scale as \code{statistic}.
}

The \code{htest} shape is used so the result prints like other R hypothesis
tests, while \code{parameter} and optional \code{bootstrap_samples} keep the settings
needed to reproduce or diagnose the p-value.
}
\description{
Runs the Kernel Stein Discrepancy goodness-of-fit test in the U-statistic
form of Liu, Lee, and Jordan (2016). The input sample is compared with a
target distribution through the target score, so the normalizing constant of
the target density is not needed.
}
\details{
Let \code{p} be the target density and let \code{k0(x, y)} be the Stein kernel built
from the selected base kernel and the supplied target score. The score is
\eqn{s_p(x) = \nabla_x \log p(x)}; because only this score is used, the
normalizing constant of \code{p} can be unknown. Liu et al. write the same kernel
as \code{u_q} because they denote the target by \code{q}; this package uses \code{k0} across
all Stein algorithms. See \code{\link[=stein_kernel_matrix]{stein_kernel_matrix()}} for the four terms that
make up \code{k0}.

The input object \code{X} represents the empirical sample. A vector is converted
to an \verb{n x 1} matrix and a matrix is interpreted row-wise, so \code{X[i, ]} is the
point \code{x_i} in the statistic. The \code{score_function} is evaluated once on this
checked matrix and must return an \verb{n x d} matrix. If \code{scaling = NULL}, the
built-in kernel scale is the median pairwise squared distance computed by
\code{\link[=find_median_distance]{find_median_distance()}}; this is a squared bandwidth for the Gaussian RBF
kernel and a squared length scale for the IMQ kernel.

For observations \eqn{x_1,\ldots,x_n}, the paper's quadratic KSD estimator is the
off-diagonal average
\deqn{U = \frac{1}{n(n - 1)} \sum_{i \ne j} k0(x_i, x_j).}
The diagonal terms \code{k0(x_i, x_i)} are left out. Under the target model this
statistic is a degenerate U-statistic, so the null distribution is not a
plain normal approximation.

The implemented test follows the Liu et al. bootstrap construction:
(1) build the \verb{n x n} Stein-kernel matrix, (2) set its diagonal to zero for
the U-statistic calculation, (3) generate centered multinomial weights
\code{w_i = N_i / n - 1 / n}, and (4) compare \code{U} with the bootstrap quadratic
forms \verb{w^T K0 w}. Both the observed and bootstrap values are kept on the
unmultiplied U-statistic scale; multiplying both by \code{n} would give the same
p-value. Large positive values mean the sample disagrees with the target
score in the reproducing-kernel Stein discrepancy.

The statistic and centered multinomial bootstrap above reproduce Liu et al.,
but the scalar p-value uses the package's finite-bootstrap convention
\deqn{p = \frac{1 + \#\{b:T_b \ge T_{obs}\}}{B+1}.}
Liu et al. Algorithm 1 instead uses the uncorrected strict exceedance
proportion \eqn{B^{-1}\#\{b:T_b>T_{obs}\}} as its rejection rule. The
\code{+1} correction prevents a zero Monte Carlo p-value and is a documented
package convention; it should not be read as a verbatim step of Algorithm 1
or as a new finite-sample guarantee for the KSD bootstrap.

Use this U-statistic version for the Liu et al. independent-sample test. For
ordered or dependent samples, \code{\link[=ksd_v_test]{ksd_v_test()}} provides the Chwialkowski et al.
V-statistic version with independent or Markov wild-bootstrap signs.
Moving down one layer from this function gives the three pieces used here:
\code{\link[=ksd_uq_matrix]{ksd_uq_matrix()}} builds the Stein-kernel matrix, \code{\link[=ksd_u_statistic]{ksd_u_statistic()}}
removes the diagonal and averages the matrix, and \code{\link[=ksd_u_bootstrap]{ksd_u_bootstrap()}}
applies centered multinomial weights to the fixed matrix. Moving sideways to
the V-statistic API replaces those three pieces by \code{\link[=ksd_vq_matrix]{ksd_vq_matrix()}},
\code{\link[=ksd_v_statistic]{ksd_v_statistic()}}, and \code{\link[=ksd_v_bootstrap]{ksd_v_bootstrap()}}.
}
\examples{
X <- matrix(rnorm(10), ncol = 1)
score_function <- function(x) -as.matrix(x)
ksd_u_test(X, score_function, nboot = 10)
}
