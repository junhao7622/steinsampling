% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stein_thinning.R
\name{stein_thinning}
\alias{stein_thinning}
\title{Stein thinning index selection.}
\usage{
stein_thinning(
  X,
  S = NULL,
  m,
  score_function = NULL,
  pre = c("sclmed", "med", "smpcov"),
  kernel = "imq",
  pre_subsample = 1000L,
  pre_subsample_method = c("first", "even", "random"),
  sclmed_log = c("m", "pre_subsample"),
  verbose_rbf_warning = TRUE,
  ...
)
}
\arguments{
\item{X}{Numeric matrix (\code{n} x \code{d}) of sample states.}

\item{S}{Optional score matrix with the same shape as \code{X}.}

\item{m}{Number of points to select.}

\item{score_function}{Optional \verb{function(X)} returning scores.}

\item{pre}{One of the paper's three preconditioners:
\code{"sclmed"}, \code{"med"}, or \code{"smpcov"}.}

\item{kernel}{\code{"imq"} (default), \code{"gaussian_rbf"}, or a \code{SteinKernel}
object, including objects made by \code{\link[=custom_stein_kernel]{custom_stein_kernel()}}.}

\item{pre_subsample}{Max rows used for the median heuristic, or an integer
vector of row indices to use directly.}

\item{pre_subsample_method}{How to choose rows when \code{pre_subsample} is a
scalar: \code{"first"} (default), \code{"even"}, or \code{"random"}.}

\item{sclmed_log}{Quantity inside the \code{sclmed} logarithm: \code{"m"} (default) or
\code{"pre_subsample"}.}

\item{verbose_rbf_warning}{Warn when using RBF instead of the paper's IMQ
default.}

\item{...}{Kernel parameters forwarded to \code{\link[=stein_kernel]{stein_kernel()}}, such as \code{c},
\code{beta}, \code{h}, \code{sigma}, or \code{sigma2} for RBF.}
}
\value{
Integer vector of length \code{m} giving selected row indices of \code{X}.
}
\description{
Implements Algorithm 1 from Riabiz et al. (2022):
at each step choose the point minimising
\verb{1/2 kP(x_i, x_i) + sum_\{j' < j\} kP(x_pi[j'], x_i)}.
}
\examples{
X <- matrix(rnorm(6), ncol = 1)
S <- -X
stein_thinning(X, S = S, m = 2, pre_subsample = 3)
}
