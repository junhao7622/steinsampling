% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gmm_model.R
\name{posteriorgmm}
\alias{posteriorgmm}
\title{Compute posterior component probabilities for a Gaussian mixture}
\usage{
posteriorgmm(model = NULL, X = NULL)
}
\arguments{
\item{model}{Gaussian mixture model created by \code{gmm()}.}

\item{X}{Numeric vector or matrix of samples.}
}
\value{
Numeric matrix with \code{nrow(X)} rows and \code{model$nComp} columns. Entry \verb{(i, k)}
is the posterior probability that sample row \code{i} came from component \code{k};
each row sums to one up to numerical rounding.
The column order matches the component order in \code{model$mu}, \code{model$sigma},
and \code{model$weights}, so the matrix can be multiplied component-by-component
with Gaussian score contributions.
}
\description{
For each observation, this function computes the probability that the
observation came from each mixture component.
}
\details{
The posterior probability for component \code{k} at observation \code{x_i} is
\deqn{
P(Z_i = k | x_i) =
\frac{w_k \phi(x_i; \mu_k, \Sigma_k)}
     {\sum_l w_l \phi(x_i; \mu_l, \Sigma_l)}.
}
The calculation is done on the log scale first, which avoids numerical
underflow when densities are very small. If every component log density is
\code{-Inf} for a row, the function stops instead of returning arbitrary
responsibilities.

These posterior probabilities are not used to classify observations in the
package; they are an intermediate quantity for \code{\link[=scorefunctiongmm]{scorefunctiongmm()}}. The
mixture score is a posterior-weighted average of component Gaussian scores.
Returning the full responsibility matrix, instead of only the most likely
component label, preserves the uncertainty needed for that weighted average.
}
\examples{
model <- gmm()
X <- rgmm(model, n = 5)
posteriorgmm(model, X)
}
