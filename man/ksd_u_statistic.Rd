% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ksd_u_test.R
\name{ksd_u_statistic}
\alias{ksd_u_statistic}
\title{Compute the KSD U-statistic from its Stein-kernel matrix}
\usage{
ksd_u_statistic(U_mat)
}
\arguments{
\item{U_mat}{Numeric square matrix of pairwise Stein kernel values.}
}
\value{
One numeric value: the off-diagonal average of \code{U_mat}. This is the same
number stored as \code{statistic} by \code{\link[=ksd_u_test]{ksd_u_test()}}, before any bootstrap p-value
is computed. It is returned as a plain number because all reproducibility
information, such as the kernel scale and bootstrap settings, belongs to the
higher-level \code{\link[=ksd_u_test]{ksd_u_test()}} object rather than to this matrix-compression
step.
}
\description{
Takes the pairwise Stein-kernel matrix and returns the off-diagonal average
used as the observed KSD statistic in the Liu et al. test.
}
\details{
If \code{U_mat[i, j] = k0(x_i, x_j)}, the diagonal is ignored and the returned
value is
\deqn{\frac{1}{n(n - 1)} \sum_{i \ne j} U_{ij}.}
This helper is useful when the matrix has already been built once and the
user wants to inspect or reuse the observed statistic separately from the
bootstrap. It is the matrix-compression layer between \code{\link[=ksd_uq_matrix]{ksd_uq_matrix()}},
which knows about samples, scores, and kernels, and \code{\link[=ksd_u_bootstrap]{ksd_u_bootstrap()}},
which knows only about a fixed Stein-kernel matrix and bootstrap weights.

In the U-statistic form the diagonal is deliberately removed. Those diagonal
entries are self-interactions \code{k0(x_i, x_i)}; they are present in the matrix
returned by \code{\link[=ksd_uq_matrix]{ksd_uq_matrix()}} because they are useful diagnostics, but they
are not part of Liu et al.'s unbiased KSD estimator. This helper makes that
convention explicit, so a user who has built a matrix by hand can apply the
same statistic used by \code{\link[=ksd_u_test]{ksd_u_test()}}.

The function is deliberately small: it only validates that the input is a
square matrix, removes the diagonal contribution, and returns the average.
Keeping this step separate from \code{\link[=ksd_uq_matrix]{ksd_uq_matrix()}} and \code{\link[=ksd_u_bootstrap]{ksd_u_bootstrap()}}
makes it possible to compare kernels or bootstrap choices while holding the
observed matrix fixed.
}
\examples{
U <- matrix(c(1, 0.2, 0.3, 0.2, 1, 0.4, 0.3, 0.4, 1), 3, 3)
ksd_u_statistic(U)
}
