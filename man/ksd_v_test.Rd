% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ksd_v_test.R
\name{ksd_v_test}
\alias{ksd_v_test}
\title{KSD goodness-of-fit test using the Chwialkowski et al. V-statistic}
\usage{
ksd_v_test(
  X,
  score_function,
  boot_method = c("rademacher", "markov"),
  scaling = NULL,
  nboot = 1000,
  change_prob = NULL,
  kernel = c("gaussian_rbf", "imq"),
  return_raw_boot = FALSE,
  median_max_samples = 2000,
  median_use_sampling = TRUE,
  median_seed = 123,
  block_size = NULL,
  block_threshold = 5000,
  imq_beta = -0.5
)
}
\arguments{
\item{X}{Numeric vector or matrix of samples.}

\item{score_function}{Function returning the target score for each row of
\code{X}.}

\item{boot_method}{One of \code{"rademacher"} or \code{"markov"}.}

\item{scaling}{Positive kernel scale. \code{NULL} uses the median squared
distance between sample pairs.}

\item{nboot}{Number of bootstrap samples.}

\item{change_prob}{Markov sign-flip probability used when
\code{boot_method = "markov"}. It must then be supplied explicitly and should
be tuned with sample size; see Details. Use \code{boot_method = "rademacher"}
for independent signs.}

\item{kernel}{Kernel choice: \code{"gaussian_rbf"}, \code{"imq"}, or a \code{SteinKernel}
object, including objects created by \code{\link[=custom_stein_kernel]{custom_stein_kernel()}}.}

\item{return_raw_boot}{Logical; if \code{TRUE}, append raw bootstrap samples to
the output.}

\item{median_max_samples, median_use_sampling, median_seed}{Options used by
the median-scale rule when \code{scaling = NULL}.}

\item{block_size, block_threshold}{Options for computing large kernel
matrices in blocks.}

\item{imq_beta}{IMQ kernel exponent.}
}
\value{
An object of class \code{htest}, implemented as a list with:
\itemize{
\item \code{statistic}: one number labeled \code{ksd_v}; this is the observed scaled
statistic \verb{n V_n}.
\item \code{p.value}: right-tail wild-bootstrap p-value using the package's
\code{(1 + exceedances) / (nboot + 1)} finite-bootstrap convention.
\item \code{method}: text naming the bootstrap method and kernel.
\item \code{data.name}: expression used for \code{X}.
\item \code{parameter}: numeric vector containing \code{nboot}, the squared kernel
\code{scaling} used, and \code{change_prob} when \code{boot_method = "markov"}.
\item \code{bootstrap_samples}: present only when \code{return_raw_boot = TRUE}; numeric
vector of wild-bootstrap statistics on the same \verb{n V_n} scale.
}

The \code{htest} shape is used so the result behaves like a standard R test
object. The \code{parameter} field records the bootstrap and kernel choices
because changing either one changes the null distribution being simulated.
}
\description{
Runs the Kernel Stein Discrepancy goodness-of-fit test in the V-statistic
form used by Chwialkowski et al. (2016). It compares samples with a target
distribution through the target score and uses a wild bootstrap to approximate
the null distribution.
}
\details{
Let \code{p} be the target density and let \code{k0(x, y)} be the Stein kernel built
from the selected base kernel and the supplied target score. The score is
\eqn{s_p(x) = \nabla_x \log p(x)}; because only this score is used, the
normalizing constant of \code{p} can be unknown. See \code{\link[=stein_kernel_matrix]{stein_kernel_matrix()}} for
the four terms that make up \code{k0}.

The sample is represented by rows of \code{X}, and \code{score_function(X)} must return
one score vector for each row. If \code{scaling = NULL}, the built-in kernel scale
is selected by \code{\link[=find_median_distance]{find_median_distance()}}. This default is a squared scale:
for Gaussian RBF kernels it is \code{h^2}, while for IMQ kernels it is \code{c^2}.

For observations \eqn{x_1,\ldots,x_n}, this test uses
\deqn{n V_n = \frac{1}{n} \sum_{i=1}^n \sum_{j=1}^n k0(x_i, x_j).}
Some papers write the unscaled V-statistic as
\eqn{V_n = n^{-2} \sum_{i,j} k0(x_i, x_j)}. This package reports and
bootstraps the scaled version \verb{n V_n}, which is the form compared with the
wild-bootstrap distribution. Unlike the U-statistic in \code{\link[=ksd_u_test]{ksd_u_test()}}, the
diagonal terms \code{k0(x_i, x_i)} are included.

The implemented algorithm is: (1) build the full Stein-kernel matrix,
including diagonal entries, (2) compute the scaled V-statistic, (3) generate
wild-bootstrap sign weights, and (4) recompute the same quadratic form with
those signs. Independent Rademacher signs are appropriate for independent
samples. The dependent-sample result in Chwialkowski et al. is narrower than
merely requiring ordered rows: it assumes a stationary tau-mixing process,
the summability condition
\deqn{\sum_{t=1}^{\infty} t^2 \sqrt{\tau(t)} < \infty,}
a Lipschitz Stein kernel \code{k0}, and the moment condition
\eqn{E[k0(Z,Z)^2] < \infty}. Geometrically ergodic Markov chains satisfy the
required mixing property only with additional moment assumptions.

Markov signs use a two-state sign chain with probability \code{change_prob} of
switching sign between neighboring observations. In the paper's asymptotic
construction this probability is a sequence \eqn{p_n = 1 / w_n} with
\eqn{w_n \to \infty} and \eqn{w_n = o(n)}; equivalently,
\eqn{p_n \to 0} while \eqn{n p_n \to \infty}. Thus \code{change_prob} must be
chosen as a function of sample size and dependence strength. The value \code{0.5}
produces independent signs and is the paper's i.i.d. setting, not a default
calibration for dependent samples; using it for dependent rows can
miscalibrate the test. Large positive statistic values are evidence that the
sample does not match the target score well.

The reported p-value is the package's finite-bootstrap convention
\deqn{\frac{1 + \#\{b:T_b \ge T_{obs}\}}{B+1}.}
Chwialkowski et al. instead state the decision rule through the empirical
bootstrap quantile. The bootstrap statistics and observed statistic here
follow the paper, but this corrected p-value is an additional finite-
simulation convention and is not a separate finite-sample validity guarantee
for dependent data.

This function is parallel to \code{\link[=ksd_u_test]{ksd_u_test()}}. Both start from \code{X},
\code{score_function}, and a Stein kernel. The U version targets Liu et al.'s
off-diagonal unbiased statistic with centered multinomial bootstrap weights.
This V version keeps diagonal self-interactions, reports the scaled statistic
\verb{n V_n}, and uses wild-bootstrap signs. Moving down one layer gives
\code{\link[=ksd_vq_matrix]{ksd_vq_matrix()}}, \code{\link[=ksd_v_statistic]{ksd_v_statistic()}}, and \code{\link[=ksd_v_bootstrap]{ksd_v_bootstrap()}}.
}
\examples{
X <- matrix(rnorm(20), ncol = 1)
score_function <- function(x) -as.matrix(x)
ksd_v_test(X, score_function, nboot = 10)
}
