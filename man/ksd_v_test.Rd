% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ksd_v_test.R
\name{ksd_v_test}
\alias{ksd_v_test}
\title{Kernel Stein Discrepancy GOF Test (V-statistics)}
\usage{
ksd_v_test(
  X,
  score_function,
  boot_method = c("rademacher", "markov"),
  scaling = NULL,
  nboot = 1000,
  change_prob = 0.5,
  kernel = c("gaussian_rbf", "imq"),
  return_raw_boot = FALSE,
  median_max_samples = 2000,
  median_use_sampling = TRUE,
  median_seed = 123,
  block_size = NULL,
  block_threshold = 5000,
  imq_beta = -0.5
)
}
\arguments{
\item{X}{Numeric vector or matrix of samples.}

\item{score_function}{Function returning score values.}

\item{boot_method}{One of \code{"rademacher"} or \code{"markov"}.}

\item{scaling}{Positive kernel scaling parameter. NULL uses median heuristic.}

\item{nboot}{Number of bootstrap samples.}

\item{change_prob}{Markov sign-flip probability. Use \code{0.5} for the iid
special case. For a dependent MCMC chain, Chwialkowski et al. (2016)
recommend thinning outside this function until lag-one autocorrelation is
below \code{0.5}, then passing the chosen \code{a_n} here, e.g. \code{0.1} for their
Figure 3 setup.}

\item{kernel}{Kernel choice: \code{"gaussian_rbf"}, \code{"imq"}, or instantiated SteinKernel.}

\item{return_raw_boot}{Logical; if TRUE, appends raw bootstrap samples to output.}

\item{median_max_samples, median_use_sampling, median_seed}{Controls for the
median-distance heuristic when \code{scaling = NULL}.}

\item{block_size, block_threshold}{Controls for blocked matrix computation.}

\item{imq_beta}{IMQ kernel exponent.}
}
\value{
An object of class \code{htest}.
}
\description{
Kernel Stein Discrepancy GOF Test (V-statistics)
}
\examples{
X <- matrix(rnorm(20), ncol = 1)
score_function <- function(x) -as.matrix(x)
ksd_v_test(X, score_function, nboot = 10)
}
