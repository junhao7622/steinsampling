% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stein_helpers.R
\name{find_median_distance}
\alias{find_median_distance}
\title{Median squared distance between sample pairs}
\usage{
find_median_distance(Z, max_samples = 2000, use_sampling = TRUE, seed = NULL)
}
\arguments{
\item{Z}{Numeric vector, matrix, or data frame of samples.}

\item{max_samples}{Maximum number of rows used when subsampling is enabled.}

\item{use_sampling}{If \code{TRUE}, use at most \code{max_samples} rows.}

\item{seed}{Optional seed used when rows are sampled.}
}
\value{
A positive numeric scalar. It is the median of the finite off-diagonal
squared Euclidean distances after any optional subsampling. The value is on
the squared-distance scale: use \code{sqrt(find_median_distance(Z))} only if a
distance-scale bandwidth is needed. If all finite pairwise squared distances
are zero, the returned value is \code{1e-5}; if no finite distance can be formed,
the returned fallback is \code{1}.
}
\description{
Computes the median squared Euclidean distance between sample rows. This is
the median-heuristic squared scale used by the built-in kernels when the user
does not supply a scale.
}
\details{
If the sample rows are \verb{z_1, ..., z_n} in \code{R^d}, the full calculation forms
the vector of all off-diagonal squared Euclidean distances
\deqn{
  D = \{ ||z_i - z_j||_2^2 : 1 \le i < j \le n \}.
}
The return value is \code{median(D)}, not its square root. Thus for one-dimensional
input \code{c(-1, 0, 2)}, the pairwise squared distances are \code{1}, \code{9}, and \code{4},
and the returned value is \code{4}. For matrix input, rows are observations and
columns are coordinates; a numeric vector is treated as an \verb{n x 1} sample.

The value is a data-dependent bandwidth proxy. In this package it is used as
a squared scale for built-in kernels when the user supplies \code{scaling = NULL}.
For the Gaussian RBF kernel this means \code{h^2 = median(D)} in
\verb{exp(-||x - y||^2 / (2 h^2))}. For the IMQ kernel it means
\code{c^2 = median(D)} in \verb{(c^2 + ||x - y||^2)^beta}. The function does not
standardize columns, so variables should already be on a comparable scale
or the user should provide a kernel scale directly.

If \code{use_sampling = TRUE} and \code{nrow(Z) > max_samples}, only \code{max_samples} rows
are sampled without replacement before the distance vector is formed. This
changes the computational cost from all \eqn{n(n - 1) / 2} pairs to all
\eqn{m(m - 1) / 2} pairs for \code{m = max_samples}. The \code{seed} argument is used
only in that subsampling case. If all rows are used, the result is
deterministic and \code{seed} has no effect.

Repeated or nearly repeated rows can make the median equal to zero. A zero
squared scale would make the built-in kernels ill-defined, so the function
returns a small positive floor value and emits a warning in that case. This
warning is usually a signal that the sample contains many duplicates or that
the chain has not moved enough for a median-distance rule to be informative.
}
\examples{
find_median_distance(c(-1, 0, 2), use_sampling = FALSE)

Z <- matrix(c(0, 0, 1, 0, 0, 2), ncol = 2, byrow = TRUE)
find_median_distance(Z, use_sampling = FALSE)

set.seed(1)
big_Z <- matrix(rnorm(300), ncol = 3)
find_median_distance(big_Z, max_samples = 20, seed = 10)
}
