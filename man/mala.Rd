% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stein_point_mcmc.R
\name{mala}
\alias{mala}
\title{Run a Metropolis-adjusted Langevin chain}
\usage{
mala(log_p, score_function, x0, h, Sigma = NULL, m_iter)
}
\arguments{
\item{log_p}{Function taking an \verb{n x d} matrix and returning \code{n} log-density
values.}

\item{score_function}{Function taking an \verb{n x d} matrix and returning an
\verb{n x d} score matrix.}

\item{x0}{Finite initial state vector.}

\item{h}{Positive step-size multiplier; the proposal covariance is
\code{h * Sigma}.}

\item{Sigma}{Symmetric positive-definite proposal preconditioner, used for
both the drift and the noise. If \code{NULL}, the identity matrix is used.}

\item{m_iter}{Number of returned chain rows, including \code{x0} in row 1.}
}
\value{
A list with:
\itemize{
\item \code{X}: chain states.
\item \code{D}: scores at those states, reused by SP-MCMC.
\item \code{log_p}: log-density values at those states.
\item \code{accept}: 0/1 indicators, with 0 in row 1.
\item \code{counts}: numbers of rows evaluated by \code{log_p} and \code{score_function};
\code{n_eval = counts$total}.
}
}
\description{
Runs the MALA transition used by SP-MCMC.
}
\details{
From the current state \code{x}, the proposal is
\deqn{y = x + (h / 2) s_p(x) Sigma + \sqrt{h} z,
      \quad z \sim N(0, Sigma),}
so the proposal covariance is \code{h * Sigma}. The function evaluates \code{log_p}
at \code{y}; \code{-Inf} rejects the proposal. Otherwise it evaluates the score at \code{y},
forms the reverse proposal, and applies the Metropolis correction. Row 1 of
the output is \code{x0}; \code{m_iter - 1} proposals follow.
}
\examples{
score <- function(X) -as.matrix(X)
log_p <- function(X) -0.5 * rowSums(as.matrix(X)^2)
mala(log_p, score, x0 = 0, h = 0.1, m_iter = 3)
}
