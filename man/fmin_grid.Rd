% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stein_points.R
\name{fmin_grid}
\alias{fmin_grid}
\title{Create the grid search used by Stein Points}
\usage{
fmin_grid(lb, ub, n0 = 100, grow = TRUE)
}
\arguments{
\item{lb, ub}{Finite lower and upper bound vectors of equal length, with
\code{ub > lb} componentwise.}

\item{n0}{Positive integer grid size, recycled across dimensions, or a
length-\code{d} vector of positive integer sizes.}

\item{grow}{A single logical value; whether to increase grid size as points
are selected.}
}
\value{
An optimizer function with signature \verb{function(objective, X_curr, t)} that
returns \code{x_min}, \code{d_min}, \code{f_min}, and \code{n_eval}. \code{x_min} is the grid row with
the smallest objective,
\code{d_min} is the target score at that row, \code{f_min} is its objective value, and
\code{n_eval} is the number of grid rows scored.
}
\description{
Creates a deterministic grid-search function for \code{\link[=stein_points]{stein_points()}}. It
evaluates every point on a Cartesian grid and returns the smallest objective
value.
}
\details{
With \code{grow = TRUE}, the grid resolution increases as the point set grows: the
per-dimension size is
\code{n0 + round(sqrt(t))}, where \code{t} is the optimizer iteration supplied by
\code{\link[=stein_points]{stein_points()}} or \code{\link[=stein_codescent]{stein_codescent()}}. It is practical mainly in low
dimension because the total number of candidates is the product of the grid
sizes across dimensions.
}
\examples{
fmin_grid(lb = -1, ub = 1, n0 = 5, grow = FALSE)
}
