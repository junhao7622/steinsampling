% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stein_points.R
\name{fmin_grid}
\alias{fmin_grid}
\title{Create the grid search used by Stein Points}
\usage{
fmin_grid(lb, ub, n0 = 100, grow = TRUE)
}
\arguments{
\item{lb, ub}{Lower and upper bounds for candidate points.}

\item{n0}{Initial grid size per dimension.}

\item{grow}{Logical; whether to increase grid size as points are selected.}
}
\value{
An optimizer function with signature \verb{function(objective, X_curr, t)}. It
evaluates the objective on a Cartesian grid and returns \code{x_min}, \code{d_min},
\code{f_min}, and \code{n_eval}. \code{x_min} is the grid row with the smallest objective,
\code{d_min} is the target score at that row, \code{f_min} is the objective value used
by \code{\link[=stein_points]{stein_points()}} to update its KSD accumulator, and \code{n_eval} is the number
of grid rows scored. Returning the same four fields as \code{\link[=fmin_mc]{fmin_mc()}} and
\code{\link[=fmin_nm]{fmin_nm()}} makes the deterministic grid search interchangeable with the
stochastic and local optimizers.
}
\description{
Creates the deterministic grid-search function used in the Stein Points
appendix. It evaluates every point on a Cartesian grid and returns the
smallest objective value.
}
\details{
With \code{grow = TRUE}, the grid size follows the paper's idea of increasing the
grid resolution as the point set grows: the per-dimension size is
\code{n0 + round(sqrt(j))}, where \code{j} is the next point index. The method is simple
and deterministic once the grid is fixed, but it is practical mainly in low
dimension because the total number of candidates is the product of the grid
sizes across dimensions.

This is the deterministic optimizer in the Stein Points family. It is useful
for small-dimensional examples and for debugging because the same inputs lead
to the same candidate set and the same selected point.
}
\examples{
fmin_grid(lb = -1, ub = 1, n0 = 5, grow = FALSE)
}
