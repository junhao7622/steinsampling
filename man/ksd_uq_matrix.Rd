% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ksd_u_test.R
\name{ksd_uq_matrix}
\alias{ksd_uq_matrix}
\title{Build the Stein-kernel matrix for the KSD U-statistic}
\usage{
ksd_uq_matrix(
  X,
  score_function,
  scaling = NULL,
  kernel = c("gaussian_rbf", "imq"),
  imq_beta = -0.5
)
}
\arguments{
\item{X}{Numeric vector or matrix of samples. A vector is treated as \verb{n x 1}.}

\item{score_function}{Function returning the target score for each row of
\code{X}.}

\item{scaling}{Positive kernel scale. \code{NULL} uses the square of the exact
all-pair median distance.}

\item{kernel}{Kernel choice: \code{"gaussian_rbf"}, \code{"imq"}, or a \code{SteinKernel}
object, including objects created by \code{\link[=custom_stein_kernel]{custom_stein_kernel()}}.}

\item{imq_beta}{IMQ kernel exponent.}
}
\value{
Numeric \verb{n x n} matrix. Row \code{i}, column \code{j} is \code{k0(x_i, x_j)} for the checked
sample matrix. The diagonal is included in the matrix because it is useful
for diagnostics, but \code{\link[=ksd_u_statistic]{ksd_u_statistic()}} ignores it when computing the
U-statistic. The matrix can be passed directly to \code{\link[=ksd_u_statistic]{ksd_u_statistic()}} and
\code{\link[=ksd_u_bootstrap]{ksd_u_bootstrap()}}.
}
\description{
Computes the pairwise Stein kernel values that are later averaged by the
U-statistic. This is the matrix-level part of the Liu et al. KSD test.
}
\details{
Entry \verb{(i, j)} of the returned matrix is \code{k0(x_i, x_j)}. The statistic
itself is not computed here. Use \code{\link[=ksd_u_statistic]{ksd_u_statistic()}} to take the paper's
off-diagonal average, \code{\link[=ksd_u_bootstrap]{ksd_u_bootstrap()}} to make bootstrap draws from this
fixed matrix, or \code{\link[=ksd_u_test]{ksd_u_test()}} to run the full test in one call. See
\code{\link[=stein_kernel_matrix]{stein_kernel_matrix()}} for the formula used to assemble \code{k0}.

This function is the first lower-level step under \code{\link[=ksd_u_test]{ksd_u_test()}}. It is
exported because the Stein-kernel matrix is often the object one wants to
inspect when diagnosing a KSD test: large diagonal terms, nearly zero
off-diagonal terms, or a poor kernel scale are visible here before the
statistic and bootstrap compress the matrix to one number.

The parallel V-statistic builder is \code{\link[=ksd_vq_matrix]{ksd_vq_matrix()}}. Both builders evaluate
the same Stein-kernel formula through \code{\link[=stein_kernel_matrix]{stein_kernel_matrix()}} and require the
same \code{X}, \code{score_function}, and kernel settings. The difference appears in
the next step: \code{\link[=ksd_u_statistic]{ksd_u_statistic()}} discards the diagonal, while
\code{\link[=ksd_v_statistic]{ksd_v_statistic()}} keeps it and reports the scaled \verb{n V_n} statistic.
The \code{q} in \code{ksd_uq_matrix()} is a historical name inherited from the KSD
literature's \code{u_q} notation; the function still uses the target score
supplied by \code{score_function}, following the package-wide target-\code{p}
convention in this manual. In other words, the returned object is the
package's \code{K0} matrix with entries \code{k0(x_i, x_j)}, not an estimate involving
a second distribution named \code{q}.
}
\examples{
X <- matrix(rnorm(5), ncol = 1)
score_function <- function(x) -as.matrix(x)
ksd_uq_matrix(X, score_function)
}
