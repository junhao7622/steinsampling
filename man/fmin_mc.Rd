% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stein_points.R
\name{fmin_mc}
\alias{fmin_mc}
\title{Create the Monte Carlo search used by Stein Points}
\usage{
fmin_mc(lb, ub, n_mc = 20, mu0 = NULL, Sigma0 = NULL, sigsq = 1, delay = 20)
}
\arguments{
\item{lb, ub}{Finite lower and upper bound vectors of equal length, with
\code{ub > lb} componentwise.}

\item{n_mc}{Positive integer number of Monte Carlo candidates.}

\item{mu0, Sigma0}{Initial Gaussian proposal mean and covariance. \code{mu0} must
be a finite length-\code{d} vector and \code{Sigma0} a finite symmetric
positive-definite \verb{d x d} matrix.}

\item{sigsq}{Finite positive local proposal variance after the delay period.}

\item{delay}{Nonnegative integer number of optimization iterations before
using local proposals.}
}
\value{
An optimizer function with signature \verb{function(objective, X_curr, t)}. It
returns a list with \code{x_min} (best candidate row), \code{d_min} (score at that
row), \code{f_min} (objective value), and \code{n_eval} (number of candidates scored).
}
\description{
Creates a candidate-search function for \code{\link[=stein_points]{stein_points()}}. It draws a finite
set of candidate points in the box and returns the candidate with the
smallest supplied objective value.
}
\details{
The returned optimizer is a function used by \code{\link[=stein_points]{stein_points()}} and
\code{\link[=stein_codescent]{stein_codescent()}}. Early iterations draw candidates from a broad Gaussian
distribution truncated to \verb{[lb, ub]}. Once \code{t} exceeds \code{delay},
the proposal becomes local: it chooses one of the current points and draws a
Gaussian perturbation with variance \code{sigsq}, again keeping only candidates in
the box.
}
\examples{
fmin_mc(lb = -1, ub = 1, n_mc = 5)
}
